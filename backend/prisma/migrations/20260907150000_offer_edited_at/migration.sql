-- Когда компания переписала условия предложения.
-- По `updatedAt` этого не узнать: он бьётся и при отправке (переход статуса
-- пишет `SENT` вторым запросом), и при любой смене статуса дальше.
-- NULL — условия не меняли с момента отправки.
ALTER TABLE "Offer" ADD COLUMN "editedAt" TIMESTAMP(3);
