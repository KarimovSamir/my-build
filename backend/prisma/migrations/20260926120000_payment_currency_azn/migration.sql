-- Валюта платежа по умолчанию — манаты, как и все цены продукта.
--
-- Платежи вне MVP (ТЗ §11), и код их не создаёт, поэтому меняется только
-- значение по умолчанию: оставь его в долларах — первая же запись платежа
-- без явной валюты ушла бы в USD при суммах в AZN.
ALTER TABLE "Payment" ALTER COLUMN "currency" SET DEFAULT 'AZN';
