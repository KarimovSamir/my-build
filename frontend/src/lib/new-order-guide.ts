import { OrderStatus, orderStatusLabels } from "@/lib/types";

/**
 * «Что будет дальше» — подсказка справа от формы создания заказа (ТЗ §4.1).
 *
 * Не украшение: клиент заполняет форму до того, как хоть раз увидел сделку,
 * и три строки объясняют, во что превратится нажатие кнопки. Название первого
 * статуса берётся из `shared/`, а не пишется в тексте: заказ появится в списке
 * ровно с этой подписью, и расхождение читалось бы как другое состояние.
 */

export interface NewOrderStep {
  /** Номер шага, как на макете: 01, 02, 03. */
  number: string;
  text: string;
}

export const newOrderSteps: NewOrderStep[] = [
  {
    number: "01",
    text: `Заказ появится в ленте компаний со статусом «${orderStatusLabels[OrderStatus.WAITING]}»`,
  },
  {
    number: "02",
    text: "Компании пришлют цену и срок — каждая по одному предложению",
  },
  {
    number: "03",
    text: "Вы выберете одно — цена сделки зафиксируется, контакты откроются",
  },
];
