/**
 * Единая точка входа для доменных типов на фронте.
 *
 * Сами типы живут в пакете `shared/` и используются и backend'ом, и фронтом —
 * дублировать их здесь нельзя (ТЗ §12.7). Этот файл только реэкспортирует,
 * чтобы в компонентах был один короткий импорт.
 */
export * from "@mybuild/shared";
