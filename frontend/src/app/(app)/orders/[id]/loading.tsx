import { DetailSkeleton } from "@/components/detail-skeleton";

/**
 * Карточка заказа на время запроса.
 *
 * Без этого файла переход по ссылке из списка блокируется до ответа API:
 * Next.js держит прежний экран, и клик выглядит как несработавший. Списки
 * такой заглушкой закрыты с Фазы 3, карточки — нет.
 *
 * Слева — описание и файлы, справа — «Объект» и «Бюджет и сроки». Остальные
 * блоки карточки зависят от роли и статуса, и обещать их скелетом нельзя.
 */
export default function OrderDetailLoading() {
  return <DetailSkeleton mainCards={2} asideCards={2} />;
}
