# progress.md — журнал подсессий

Рабочий журнал между подсессиями. План подфаз — `PLAN.md`. Правила проекта — `CLAUDE.md`. Требования — `MyBuild_NestJS_TZ.md`.

---

## Как этим пользоваться

**В начале подсессии агент обязан:**

1. Прочитать этот файл целиком (начиная с последней записи).
2. Посмотреть в `PLAN.md`, какая подфаза идёт следующей.
3. Прочитать раздел ТЗ, к которому относится подфаза.
4. Взять **ровно одну** подфазу. Не больше.

**В конце подсессии агент обязан:**

1. Дописать запись по шаблону ниже — четыре блока, ни один не пропускать.
2. Дать в чат короткий отчёт и указать: «в фазе N осталось: …» либо «фаза N завершена целиком».
3. Остановиться. Не задавать вопрос «делать дальше?». Следующая подфаза — только по явному указанию пользователя.

**Правила для записей:**

- Новая запись добавляется **вниз** файла. Прежние не переписывать и не сокращать.
- «Сделано» пишется только после проверки (`CLAUDE.md` §2). Тесты упали — так и написать, с выводом.
- Незаконченное уходит в «что дальше», а не замалчивается.
- Даты — абсолютные.

---

## Шаблон записи

```markdown
## Подфаза X.Y — <название> — <дата>

**Сделано**
- Что именно и где лежит (файлы, маршруты, миграции).
- Чем проверено: какие тесты прогнаны, какие endpoint'ы дёрнуты, какие экраны открыты.

**Принятые решения**
- Развилка → выбранный вариант → почему. Если решение принял пользователь — так и написать.

**Что дальше**
- Следующая подфаза по `PLAN.md`.
- Что из текущей подфазы осталось незакрытым и почему.

**Где грабли**
- На что напоролись, что не очевидно из кода, что сломается у следующего агента, если он этого не знает.
```

---

## Записи

## Фазы 0–2 — завершены — 2–3 сентября 2026

Подробности не дублируются: они в `CLAUDE.md` §6 («Что уже сделано»), там же «что всплыло» по каждой фазе и принятые по ходу решения.

Коротко, что есть на входе в Фазу 3:

- Монорепозиторий npm workspaces: `backend/` (NestJS 12, ESM), `frontend/` (Next.js 16 + Tailwind 4 + shadcn/ui), `shared/` (`@mybuild/shared` — enum-ы, цвета и названия статусов, типы API).
- Prisma-схема всех сущностей §3, миграции применены, RLS без политик на всех таблицах `public`.
- `OrderStateMachine` (чистая функция) + `OrderTransitionService` (одна транзакция на переход, блокировка строки заказа, уведомления тем же коммитом). Возвращает `fromStatus/nextStatus/notifications` — из этого Фаза 5 соберёт WebSocket-события.
- Supabase Auth: триггер профиля, `CHECK` на `companyName`, Custom Access Token Hook с claim `user_role`. На backend — `SupabaseAuthGuard` и `RolesGuard` глобально через `APP_GUARD`, `@Public()`, `@Roles`, `@CurrentUser`, `GET`/`PATCH /profile`. На фронте — `proxy.ts`, страницы входа/регистрации/сброса пароля, `/callback`.
- Тесты: 92 unit, 18 e2e. Seed идемпотентен, аккаунты заводятся через Supabase Admin API.

**Открытые вопросы к пользователю** — `CLAUDE.md` §7.1 и §7.2. Оба напоминаются в подфазе 7.4, раньше не поднимать.

**Грабли, которые действуют дальше всех фаз:**

- Prisma CLI (`migrate`, `db seed`) не работает в песочнице агента — падает с `P1001`. Запускать без песочницы.
- Прямая вставка в `public."User"` невозможна из-за внешнего ключа на `auth.users`: пользователей заводить только через Admin API.
- Файлы в seed существуют только в БД, в бакете Storage их нет — скачивание по signed URL на seed-данных не сработает. Учесть в 3.1.
- e2e ходит в реальную Supabase (~14 с на прогон), тестовые адреса в домене `@e2e.test`.
- REST-эндпоинт Supabase (PostgREST) отдаёт 503 `PGRST002`. На приложение не влияет — мы этим путём не ходим.

**Что дальше:** подфаза 3.1 — модуль `files` (Supabase Storage). Начинать только по указанию пользователя.

---

## Подфаза 3.1 — модуль `files`: Supabase Storage — 3 сентября 2026

**Сделано**

- **Бакет.** Приватный бакет `order-files` в Supabase (уже существовал, скрипт привёл настройки к нужным: `public: false`, лимит 20 МБ). Заводится командой `npm run storage:setup -w backend` → `backend/scripts/ensure-storage-bucket.ts`, идемпотентна. Упомянута в `backend/env.example`.
- **`src/supabase/supabase.module.ts`** — клиент Supabase с секретным ключом как провайдер Nest (токен `SUPABASE_ADMIN`). Не глобальный: импортирует только тот, кому он нужен. Seed и e2e по-прежнему берут клиент напрямую из `supabase-admin.ts` — там контейнера DI нет.
- **`src/modules/files/file-validation.ts`** — чистые функции: `prepareFile` (размер по содержимому, 400 на пустой, 413 на >20 МБ, SHA-256), `resolveMimeType`, `sanitizeFileName` (транслит кириллицы, отрезание пути, схлопывание символов, ограничение длины).
- **`src/modules/files/storage.service.ts`** — обёртка над бакетом: `upload` (с `upsert`, потому что в ключе лежит хеш содержимого), `createSignedUrl` (TTL 300 с, `download` → Content-Disposition), `remove` (ошибку не бросает, пишет в лог).
- **`src/modules/files/files.service.ts`** — `attachFiles` (валидация → дедуп внутри пачки и внутри сдачи → загрузка → `createManyAndReturn` со `skipDuplicates` → уборка объектов, чьи строки не создались), `getDownloadUrl`, `listOrderFiles`, `assertOrderParticipant`, `removeStorageObjectsForOrder`. Ключ объекта: `orders/{orderId}/{client|company}/{round}/{хеш16}-{имя}`.
- **`FilesModule`** подключён в `app.module.ts`. Своих маршрутов нет: загрузка живёт на маршрутах заказа (3.2 и 4.2), раздел «Документы» переиспользует сервис в 6.1.
- **Проверено:** 123 unit-теста (было 92; новые — `file-validation.spec.ts` и `files.service.spec.ts`), 27 e2e (было 18; новый `test/files.e2e-spec.ts` — 9 тестов на живом Supabase), `npm run typecheck`, `npm run lint`, `npm run build` всего репозитория — всё зелёное.
- **Ручная проверка вне тестов** (временный скрипт, удалён): настоящий PNG 15 320 байт → строка `OrderFile` создана → объект виден в бакете тем же размером → signed URL отдал 200 и побайтово те же данные → `removeStorageObjectsForOrder` очистил папку. Бакет после всех прогонов пуст.

**Принятые решения**

- **Тип файла определяется расширением, а не заголовком запроса.** Браузер для DWG почти всегда шлёт `application/octet-stream` — по строгому allowlist'у DWG нельзя было бы загрузить вовсе, хотя ТЗ §5 его требует. Поэтому: расширение обязано быть в таблице, а заявленный MIME либо совпадает с ним (с учётом синонимов вроде `application/acad`), либо не распознан клиентом. Подмена (`.exe` под видом PDF) отсекается расширением, `.pdf` с чужим типом — сверкой. В базу пишется канонический тип, а не то, что назвал клиент.
- **Доступ к файлам: клиент заказа либо компания-исполнитель.** Список статусов предложения свой — `ACCEPTED / WORK_SUBMITTED / BACK_FOR_OVERRIDE / COMPLETED`; он отличается от `EXECUTING_OFFER_STATUSES` в `OrderTransitionService` добавленным `COMPLETED`: после сдачи компания должна сохранять доступ к тому, что сама загружала. Компания со статусом `SENT` файлов не видит (ТЗ §4.1: приватность).
- **Проверка по идентификатору, а не по роли.** Роль в токене живёт час и может устареть, связь с заказом — нет.
- **Из одинаковых файлов в пачке остаётся первый**, а не последний: пользователь видит то имя, под которым приложил файл.
- **Имя для скачивания транслитерируется, если в нём не только ASCII** (`toDownloadName`). Причина — в граблях ниже. Настоящее имя не теряется: оно в `OrderFile.originalName` и показывается в интерфейсе.
- **`UploadedFileInput` — своя форма, не multer'овская.** Сервис не зависит от транспорта, и unit-тесты не поднимают Express.
- **Уборка бакета — отдельный метод, а не каскад.** Строки `OrderFile` уходят каскадом за заказом, объекты в хранилище — нет; 3.2 обязана вызвать `removeStorageObjectsForOrder` перед удалением заказа.

**Что дальше**

- Подфаза **3.2** — backend `orders`: CRUD (`POST /orders` multipart, `GET /orders`, `GET /orders/:id`, `DELETE /orders/:id`, `OwnershipGuard`, throttler).
- Незакрытого из 3.1 нет. Маршрутов у модуля files нет намеренно — они появятся вместе с заказами.

**Где грабли**

- **Supabase ломает кириллицу в имени скачиваемого файла.** Параметр `download` он не декодирует, а в Content-Disposition подставляет как есть: имя доезжает percent-кодированным дважды (`Логотип.png` → `%D0%9B%D0%BE…`). Обойти со стороны клиента нельзя — `fetch` и браузер кодируют значение в URL сами. Поэтому не-ASCII имена уходят транслитерированными. Если Supabase починит — снять `toDownloadName` в `files.service.ts`.
- **Multer портит UTF-8 в `originalname`.** Express-multer читает имя из multipart как latin1. В 3.2 на границе контроллера имя нужно чинить: `Buffer.from(file.originalname, 'latin1').toString('utf8')`, иначе в базу попадут «ÐÐ»Ð°Ð½.pdf». Проверить это тестом на настоящей загрузке через HTTP.
- **Скрипты на `tsx` не могут поднять Nest.** `tsx` (esbuild) не умеет `emitDecoratorMetadata`, поэтому `NestFactory.create*(AppModule)` падает уже на валидации `.env` (`PORT must be an integer`: без метаданных class-transformer не преобразует строку в число). В скриптах сервисы собираются руками, как в seed. Отдельно: при `{ logger: false }` Nest съедает эту ошибку и процесс молча выходит с кодом 1 — отлаживать только с включённым логгером.
- **Бакет не создаётся сам.** На свежем проекте Supabase перед первой загрузкой нужен `npm run storage:setup -w backend` — как миграции для базы.
- **Файлы seed по-прежнему существуют только в БД** (грабли Фазы 1 в силе): у их строк `OrderFile` нет объектов в бакете, поэтому `getDownloadUrl` на seed-данных вернёт ссылку, по которой ничего не скачается. Чинить в seed не стали: это работа не 3.1, а того момента, когда seed начнёт грузить настоящие файлы.
- **Дедупликация не защищает от гонки полностью.** Два одновременных запроса с одним файлом ловятся уникальным ограничением базы (`orderId + submissionRound + fileHash`), `skipDuplicates` не даёт запросу упасть, а лишний объект убирается по разнице ключей.

---

## Подфаза 3.2 — backend `orders`: CRUD — 3 сентября 2026

**Сделано**

- **Маршруты** (`src/modules/orders/orders.controller.ts`), все четыре из ТЗ §5:
  - `POST /orders` — multipart с файлами, роль `CLIENT`, статус `WAITING`, файлы получают `ownerType = CLIENT` и `submissionRound = 0`. Ограничение частоты: 20 запросов в минуту.
  - `GET /orders?status=&q=&page=&pageSize=` — свои заказы, фильтр по статусу, поиск, пагинация, роль `CLIENT`.
  - `GET /orders/:id` — обе роли, ответ ролезависимый.
  - `DELETE /orders/:id` — роль `CLIENT`, только свой заказ и только в `WAITING` / `AWAITING_CONFIRMATION`, ответ 204. Ограничение: 30 в минуту.
- **`OwnershipGuard`** (`src/common/guards/ownership.guard.ts`) — первое применение, ТЗ §9. Режим задаётся декоратором `@OrderAccess(OWNER | VIEWER)`; найденный заказ кладётся в запрос и достаётся в контроллере через `@OrderAccessCtx()`, чтобы сервис не повторял тот же запрос к базе.
- **`ThrottleGuard`** (`src/common/guards/throttle.guard.ts`) + `@Throttle({ limit, ttl })` — своя реализация вместо `@nestjs/throttler` (причина ниже, решение принял пользователь). Скользящее окно в памяти по «пользователь + маршрут», 429 с `Retry-After`.
- **`order-view.ts`** — чистые функции `toOrderListItem` / `toOrderDetail` / `resolveVisibility`: вся приватность ТЗ §4.1 в одном месте, без базы и без Nest.
- **`order-participation.ts`** — `EXECUTOR_OFFER_STATUSES` и `ACTIVE_OFFER_STATUSES`. `FilesService` теперь берёт список оттуда же, а не держит свою копию.
- **`OrdersService`** — создание с откатом заказа при отказе на файлах, список с поиском (`buildSearchConditions`), детали, удаление с уборкой бакета.
- **DTO** — `CreateOrderDto` и `ListOrdersQueryDto` с русскими сообщениями валидации, зеркало для формы Фазы 3.4.
- **`FilesService`** — добавлены `listStorageKeys` и `removeStorageObjects`; `removeStorageObjectsForOrder` теперь через них. Нужно для правильного порядка удаления заказа.
- **Проверено:** 148 unit-тестов (было 123; новые — `order-view.spec.ts` на приватность и `throttle.guard.spec.ts`), 59 e2e (было 27; новый `test/orders.e2e-spec.ts` — 32 теста на живой Supabase), `npm run typecheck`, `npm run lint`, `npm run build` всего репозитория — всё зелёное.
- **Ручная проверка вне тестов:** собранный backend (`node dist/main.js`) поднят, `/health` отдал `database: "up"`, все четыре маршрута смонтированы и без токена отдают 401. С настоящими токенами seed-пользователей проверены на живых данных: список с пагинацией (`total: 6`), поиск `ORD-24`, поиск по подрядчику «СтройГрад» (2 заказа), детали глазами клиента и глазами компании-исполнителя, 403 на `GET /orders` от компании.

**Принятые решения**

- **`@nestjs/throttler` не поставлен — написан свой guard. Решение пользователя от 3 сентября 2026.** Последняя версия библиотеки (6.5.0) объявляет peer-зависимость только до NestJS 11, а в проекте 12; `overrides` в npm на peer-конфликты не действуют, оставался только репозиторный `legacy-peer-deps`, который отключил бы проверку совместимости во всём репозитории. Свой guard — ~110 строк, покрыт unit-тестами, без зависимости. В Фазе 7.3 при желании меняется на библиотеку правкой одного модуля.
- **Чужой заказ отдаётся как 404, а не 403** — и на `GET`, и на `DELETE`. В `PLAN.md` для 3.2 было написано «403 на чужой заказ», но 403 подтверждает существование заказа с таким идентификатором. 403 в наборе остался там, где он честен: чужая роль (компания на маршрутах клиента). Подфаза 3.5 требует 404 на странице деталей — это тот же выбор.
- **Компания видит любой заказ, но без прогресса.** ТЗ §4.1 сформулировано как «для неё такой заказ выглядит как `WAITING`», то есть заказ видим, а урезан состав: статус подменяется на `WAITING`, цена, срок, подрядчик, уточнённая площадь, комментарии, файлы и чужие предложения не отдаются. Задание клиента (описание, адрес, площадь, бюджет) остаётся видимым — по нему компания и подаёт предложение.
- **Активное предложение = `SENT` + статусы исполнителя.** Компания с `SENT` видит настоящий статус заказа, но не файлы: файлы даются только исполнителю, как в `FilesService` из 3.1. Компания с `WITHDRAWN` / `REJECTED` / `NOT_ACCEPTED` видит заказ как посторонняя, но своё выбывшее предложение — по-прежнему видит.
- **Клиенту показываются только предложения, которые ещё в игре** (`SENT` + статусы исполнителя). Отозванные, отклонённые и невыбранные ему уже ни на что не влияют (ТЗ §4.1).
- **Порядок при создании: сначала строка заказа, потом файлы, при отказе — откат.** Ключ объекта в хранилище содержит `orderId`, поэтому иначе не выходит. Побочный эффект: номера заказов идут с пропусками — для autoincrement нормально и пользователю не видно.
- **Порядок при удалении: ключи → удаление строки → уборка бакета.** Строки `OrderFile` уходят каскадом, и после удаления узнать, что убирать из хранилища, уже неоткуда.
- **`ThrottleGuard` на `DELETE` идёт перед `OwnershipGuard`** — ограничитель дешевле похода в базу и заодно тормозит перебор идентификаторов.

**Что дальше**

- Подфаза **3.3** — frontend: список заказов (браузерный API-клиент, `/orders`, поиск, вкладки статусов, таблица → карточки на мобильном, `StatusBadge`, skeleton, empty-state).
- Незакрытого из 3.2 нет.
- К Фазе 7.3 вернуться к двум вещам из этой подфазы: счётчики `ThrottleGuard` живут в памяти процесса (при нескольких экземплярах backend'а лимит станет общим только внутри каждого), и за обратным прокси для настоящего IP анонимных запросов нужен `app.set('trust proxy', 1)`.

**Где грабли**

- **`enableImplicitConversion` ломает необязательные поля типа `Date`.** Глобальный `ValidationPipe` преобразует значение по объявленному типу поля *до* собственных `@Transform`. Пустая строка из multipart (браузер шлёт незаполненные поля именно так) превращалась в `Invalid Date`, и своё преобразование получало уже её, а не `''`. Поэтому `desiredStartDate` объявлен как `string` с `@IsDateString` и своим валидатором `IsNotPastDate`, а в `Date` его превращает сервис. Для любого нового необязательного поля-даты — так же.
- **`defParamCharset: 'utf8'` в `FilesInterceptor` обязателен.** По умолчанию multer читает имена файлов из multipart как latin1. Это те самые грабли из 3.1 — чинить `Buffer.from(name, 'latin1')` вручную не понадобилось, опция решает вопрос на входе. Проверено e2e: «План квартиры.pdf» доезжает целым.
- **`@types/multer` не ставили.** В контроллере объявлен свой интерфейс на три поля. Если понадобится больше — не тянуть пакет ради типа, а дописать интерфейс.
- **Порядок сортировки в тестах не проверить на параллельно созданных заказах.** `Promise.all` из трёх `prisma.order.create` даёт непредсказуемый порядок `createdAt`, и тест на «новые сверху» падал через раз. В `orders.e2e-spec.ts` фикстуры создаются строго по очереди.
- **e2e на ограничитель частоты берёт отдельного пользователя.** Тест выбирает лимит `POST /orders` целиком, и на общем пользователе он сломал бы остальные тесты файла. Окно живёт в памяти приложения, между файлами e2e не переносится (приложение поднимается заново), но внутри файла — переносится.
- **Прогон e2e вырос до ~36 секунд** (было ~14): добавились 32 теста, ходящих в реальную Supabase. Таймаутов в `vitest.config.e2e.ts` (60 с) хватает с запасом.

---

## Подфаза 3.2 — доработка по итогам ревизии — 3 сентября 2026

Пользователь спросил, осталась ли в 3.2 явная проблема. При целенаправленной проверке нашлась одна настоящая — исправлена, плюс подчищено одно место, которое маскировало ошибки.

**Сделано**

- **Исправлено: поиск по длинной строке цифр отдавал 500.** `Order.orderNumber` — колонка `Int` (32 разряда), а `parseOrderNumber` пропускал любое безопасное целое. Запрос `?q=99999999999` доходил до базы и падал там с `PrismaClientKnownRequestError: value out of range for type integer`, который `AllExceptionsFilter` превращал в 500 «Внутренняя ошибка сервера». Воспроизведено на живой базе до правки.
  - В `shared/src/labels.ts` добавлена константа `MAX_ORDER_NUMBER = 2_147_483_647`, и `parseOrderNumber` теперь возвращает `null` для всего, что вне диапазона `1…MAX_ORDER_NUMBER`. Правка именно в `shared/`: тем же разбором в 3.3 будет пользоваться поиск на фронте, и расходиться им нельзя.
  - Построение условий поиска вынесено из `orders.service.ts` в чистый модуль `src/modules/orders/order-search.ts` — чтобы правило проверялось unit-тестом, а не только прогоном по живым данным.
- **Убрано маскирование ошибки при откате создания заказа.** Если загрузка файлов отклонена, заказ удаляется; раньше отказ самого удаления затирал исходную причину, и понятный 400 «такой тип загрузить нельзя» превращался бы в 500. Теперь откат best-effort с записью в лог, наружу уходит исходная ошибка.
- **Проверено:** 155 unit-тестов (было 148; новый `order-search.spec.ts` — 7 тестов), 60 e2e (было 59; новый тест на длинную строку цифр в поиске), типы, линтер и сборка всего репозитория.

**Отдельно проверено и оказалось в порядке**

- Пагинация переполнением не ломается: `skip` уходит в `OFFSET` как 64-разрядное, `page=1e15` отрабатывает без ошибки.
- Отрицательная площадь, неизвестная категория, пустые необязательные поля, дата в прошлом, файл чужого типа — всё отсекается валидацией и покрыто e2e.

**Что дальше**

- Подфаза **3.3** — frontend: список заказов. План не изменился.
- Незакрытого из 3.2 нет.

**Где грабли**

- **Любое число из пользовательского ввода, которое идёт в колонку `Int`, надо ограничивать до похода в базу.** Prisma не приводит его к диапазону и не даёт внятной ошибки — запрос падает в Postgres, а наружу уходит 500. В Фазах 4–6 это касается всего, что будет искать по номерам.
- **`orderNumber` теперь ограничен и в `shared/`.** Если когда-нибудь колонка станет `BigInt`, править надо `MAX_ORDER_NUMBER`, а не только схему.

---

## Подфаза 3.3 — frontend: список заказов — 3 сентября 2026

**Сделано**

- **`/orders`** (`frontend/src/app/(app)/orders/page.tsx`) — заголовок с кнопкой «Создать заказ», карточка с поиском и вкладками, под ней список. Список вынесен под `<Suspense>` с ключом по фильтру: при смене выборки скелет показывается только на месте таблицы, а поле поиска не перерисовывается и не теряет фокус.
- **`lib/orders-filter.ts`** — состояние фильтра (статус, строка поиска, страница) живёт в адресе страницы. Разбор параметров, сборка адреса (`ordersHref`), порядок вкладок и ключ для `<Suspense>` собраны в одном месте, чтобы страница, поиск и вкладки понимали параметры одинаково. Всё непонятное в адресе молча заменяется значением по умолчанию.
- **`lib/format.ts`** — `formatMoney` («45 000 USD», ТЗ §7) и `formatDate` («25 дек 2025»).
- **`components/orders/orders-search.tsx`** — клиентский компонент: ввод с задержкой 350 мс, затем `router.replace` на новый адрес, страница сбрасывается на первую. Значение поля синхронизируется с адресом только тогда, когда адрес изменили снаружи (сброс фильтров, кнопка «назад»), — иначе поле затирало бы набираемый текст.
- **`components/orders/orders-status-tabs.tsx`** — вкладка «Все заказы» плюс шесть статусов из `shared/`. Это ссылки, а не переключатель на состоянии: выборка обязана оставаться в адресе. Полоса вкладок горизонтально прокручивается на узких экранах.
- **`components/orders/orders-list.tsx`** — серверный асинхронный компонент: запрос к `GET /orders` через `serverApi`, таблица (Заказ / Подрядчик / Статус / Бюджет / Срок) на десктопе и карточки на мобильном, пагинация «Показано N из M» с кнопками «Назад» / «Вперёд», три пустых состояния (заказов нет вовсе / ничего не найдено / страница за пределами выборки).
- **`components/orders/orders-list-skeleton.tsx`** — скелет той же высоты и ритма, что настоящие строки.
- **Проверено:** 155 unit-тестов и 60 e2e (backend не менялся, прогон для контроля), `npm run typecheck`, `npm run lint`, `npm run build` всего репозитория — зелёное.
- **Проверено на живых данных.** Поднят собранный backend (`node dist/main.js`, `/health` → `database: "up"`) и `next start`; вход выполнен настоящим SDK (`@supabase/ssr`, `signInWithPassword`) под seed-клиентом, страницы вытянуты с полученной cookie сессии. Результат в отрендеренном HTML: список из 6 заказов с номерами `ORD-19…24`, подрядчиками и статусами; вкладка `status=COMPLETED` → 1 заказ; поиск `ORD-21` → 1 заказ; поиск «СтройГрад» → 2 заказа; `q=zzzz` → «Ничего не найдено»; `page=2` → «На этой странице заказов нет» и «Показано 0 из 6»; мусор в адресе (`status=WRONG&page=abc`) → полный список, первая страница. Отдельно заведён и удалён временный клиент без заказов — у него «Заказов пока нет» с кнопкой «Создать заказ». Компания на `/orders` получает 307 на `/available`. В логе Next ошибок и предупреждений нет.

**Принятые решения**

- **Список рендерится на сервере, а не в браузере.** `PLAN.md` предлагал начать 3.3 с браузерного API-клиента, но для этого экрана он не нужен: фильтр живёт в адресе, данные приходят вместе со страницей, ссылку на выборку можно переслать, а кнопка «назад» возвращает предыдущий фильтр. Браузерный клиент появится в 3.4, где он нужен по-настоящему — форма создания заказа грузит файлы multipart прямо из браузера. Заводить его сейчас значило бы положить в репозиторий модуль без единого вызывающего.
- **Живое обновление Фазы 5 это не ломает:** по событию сокета делается `router.refresh()`, и серверный компонент перезапрашивает список без перезагрузки страницы.
- **Вкладок семь: «Все» и шесть статусов**, а не три, как на макете. API принимает ровно один статус, группировки («В работе», «Завершённые») там нет, а ТЗ §4.1 требует фильтр по статусу. Макет ниже ТЗ по приоритету.
- **В колонке «Бюджет» всегда подписано, какое из двух чисел показано.** ТЗ §3 запрещает смешивать `clientBudget` (ожидание клиента) и `price` (цена сделки). Показывается `price`, если он есть, иначе `clientBudget`, и под суммой мелким шрифтом — «цена сделки» или «бюджет клиента».
- **Колонка «Срок» — это `deadline` из принятого предложения**, поэтому до принятия там прочерк. Желаемой даты начала в `OrderListItem` нет, и добавлять её значило бы менять контракт API — это вопрос к пользователю, а не решение подфазы.
- **Кликается вся строка**: `<tr>` получает `position: relative`, а ссылка в первой ячейке растягивается на строку (`after:absolute after:inset-0`). Вкладывать `<a>` вокруг `<tr>` нельзя. Если бы приём не сработал, деградация мягкая — ссылка на названии заказа остаётся рабочей.
- **Отдельного `loading.tsx` нет.** Скелет даёт `<Suspense>` вокруг списка: так шапка с поиском остаётся на экране, а `loading.tsx` подменял бы страницу целиком и на каждой букве отбирал бы фокус у поля.

**Что дальше**

- Подфаза **3.4** — форма создания заказа: две колонки, `FileDropzone`, валидация на русском, браузерный API-клиент (`lib/api.ts` уже транспортно готов, нужна обёртка, берущая токен из сессии Supabase в браузере).
- Из состава 3.3 не сделан браузерный API-клиент — сознательно перенесён в 3.4 (причина выше). Больше незакрытого нет.
- **Не проверено в настоящем браузере:** задержка ввода в поиске, клик по строке таблицы, вид на мобильной ширине и отсутствие ошибок гидратации. Всё это проверялось по отрендеренному HTML, а не глазами: браузера у агента в этой сессии не было. Стоит открыть `/orders` руками до начала 3.4.

**Где грабли**

- **Git Bash подменяет аргумент вида `/orders` на путь Windows** (`C:/Program Files/...`). Аргументы-пути к скриптам проверки передавать из PowerShell либо со строкой запроса, иначе ломается непонятным образом.
- **`@supabase/ssr` не импортируется из ESM-скрипта вне репозитория:** его `module`-сборка написана под бандлер и содержит импорты без расширений. В скриптах брать CJS-сборку через `createRequire`.
- **Размер страницы фиксирован (20, дефолт backend).** Экран не отдаёт `pageSize`, поэтому на seed-данных (6 заказов) вторая страница не набирается, и пагинация «вперёд» проверялась только через ручной `?page=2`. Когда заказов станет больше, перепроверить.
- **Поиск теряет недонабранный текст при клике по вкладке:** ссылка вкладки собирается из значения в адресе, а не из того, что сейчас в поле. Заметно только если кликнуть по вкладке в течение 350 мс после набора.
- **`shared/` собирается отдельно.** Новые константы и типы из `shared/src` не видны фронту, пока не выполнен `npm run build:shared` (в корневом `prepare` это есть, но при правке `shared/` по ходу работы про это легко забыть).

---

## Подфаза 3.4 — frontend: создание заказа — 3 сентября 2026

**Сделано**

- **`/orders/new`** (`frontend/src/app/(app)/orders/new/page.tsx` + `components/orders/new-order-form.tsx`) — две колонки по макету: «Детали проекта» (название, категория, тип объекта, площадь, описание, файлы) и «Бюджет и локация» (бюджет, желаемая дата начала, адрес). Кнопки «Отмена» / «Опубликовать заказ», тост об успехе, переход на созданный заказ, `router.refresh()` следом (список рендерится на сервере и иначе остался бы в кэше роутера без новой строки).
- **`lib/api.client.ts`** — браузерный клиент к API, зеркало `api.server.ts`: токен берётся из сессии Supabase в браузере, вызывающий код о нём не знает. Появился здесь, потому что форма шлёт multipart с файлами прямо из браузера.
- **`lib/order-form.ts`** — чистый модуль формы: `validateOrderForm` (зеркало `CreateOrderDto`, сообщения русские), `toOrderFormData` (сборка multipart; незаполненные необязательные поля не отправляются — `forbidNonWhitelisted`), `addFiles` / `fileRejectionReason` (тип, размер, количество, дедуп по имени и размеру), `formatFileSize`, `todayIsoDate`.
- **`components/orders/file-dropzone.tsx`** — drag&drop и выбор через диалог, превью картинок (object URL со снятием), удаление файла до отправки, отказы списком под областью.
- **`components/date-picker.tsx`** — Popover + Calendar, локаль `ru`, дни раньше сегодняшнего отключены, кнопка «Очистить» (поле необязательное). Значение — строка `ГГГГ-ММ-ДД`, а не `Date`.
- **Общие константы вынесены в `shared/`** — `ORDER_LIMITS` и `MONEY_PATTERN` (новый `shared/src/orders.ts`), `FILE_EXTENSION_MIME`, `ALLOWED_FILE_EXTENSIONS`, `ALLOWED_FILE_EXTENSIONS_HINT`, `MAX_FILES_PER_REQUEST`, `fileExtension` (в `shared/src/api.ts`). Backend переведён на них: `CreateOrderDto`, `file-validation.ts`, `orders.controller.ts`. Правил не менял — только перенёс, чтобы форма и DTO не разъехались.
- **`components/auth/form-parts.tsx` → `components/form-parts.tsx`** — файл общий для всех форм, а не только для авторизации; добавлены ошибка поля (`error`) и `FieldMessage` для полей со своим управляющим элементом (Select, календарь, dropzone). Импорты в четырёх формах авторизации обновлены.
- **Проверено:** 155 unit-тестов и 60 e2e (backend), `npm run typecheck`, `npm run lint`, `npm run build` всего репозитория — зелёное.
- **Проверено в настоящем браузере** (Chrome DevTools MCP, seed-клиент Анна Смирнова, собранные backend и frontend):
  - Пустая форма → ошибки под всеми шестью обязательными полями, `aria-invalid`, «Проверьте выделенные поля» над кнопкой.
  - Файл `virus.exe` → «такой тип загрузить нельзя», в список не попал. Повторный выбор того же `plan.png` не продублировал строку.
  - Заполненная форма с «План квартиры.pdf» и `plan.png` → заказ `ORD-111`, тост, переход на `/orders/{id}`, заказ найден поиском в списке.
  - Ответ `GET /orders/:id`: `squareMeters: 62.5` (вводилось «62,5»), `clientBudget: "31000.5"`, `desiredStartDate: 2026-09-15`, оба файла с `ownerType: CLIENT`, `submissionRound: 0`, кириллическое имя целое.
  - Объекты в бакете: `orders/{id}/client/0/794abaa4f6f06fc5-plan-kvartiry.pdf` и `…-plan.png` (проверено временным скриптом, он удалён).
  - Остановленный backend → «Не удалось отправить заказ. Проверьте соединение…», данные формы на месте, повторная отправка после запуска backend прошла.
  - Оба тестовых заказа удалены через `DELETE /orders/:id`, база вернулась к seed-состоянию (6 заказов), бакет по заказу пуст.
  - Мобильная ширина 390 px — одна колонка, ничего не обрезано; десктоп 1440 px — раскладка как на макете. Консоль без ошибок и предупреждений.

**Принятые решения**

- **Общие правила формы уехали в `shared/`, а не продублировались.** `CLAUDE.md` §5 запрещает дублировать типы и enum руками, и те же грабли здесь: разъехавшись, форма и DTO дали бы либо ложную ошибку, либо 400 после отправки. Заодно `EXTENSION_MIME` из `file-validation.ts` стал `FILE_EXTENSION_MIME` в `shared/` — по нему фронт отбирает файлы в диалоге и отсеивает лишние.
- **Дата сравнивается по UTC и на фронте, и на бэке.** `CreateOrderDto` считает «сегодня» по UTC; календарь запрещает дни раньше того же UTC-сегодня. Иначе западнее Гринвича форма предлагала бы день, который сервер отклонит.
- **Своя валидация на `useState`, без react-hook-form и zod.** Формы авторизации написаны так же, набор правил маленький, а новая зависимость ради одной формы — лишний вес и лишний повод для конфликта версий.
- **Отправка из браузера, а не серверным действием.** Файлы уходят в API напрямую; server action заставил бы гнать те же 20 МБ сначала в процесс Next.js, а потом в NestJS.
- **Ошибки API показываются списком над кнопкой, а не под полями.** Сообщения валидации приходят строками без имени поля — привязать их к полю нечем. Под полями показывается то, что форма нашла сама до отправки.
- **Запятая в числах принимается** (`62,5` → `62.5`): для русского интерфейса это привычный разделитель, а backend ждёт точку.
- **«Отмена» — ссылка на `/orders`, а не `router.back()`.** На форму приходят и по прямому адресу, и тогда «назад» уводит неизвестно куда.
- **Подпись «Файлы» связана с кнопкой dropzone** (`label htmlFor` → `button id`), скрытому `input[type=file]` дано имя, адресу — `autocomplete="street-address"`. Без этого Chrome писал в консоль замечания к разметке формы.

**Что дальше**

- Подфаза **3.5** — детали заказа: описание, площадь и уточнённая площадь, адрес, сроки, бюджет/цена, файлы клиента со скачиванием, размеченные пустые блоки под Фазу 4, удаление заказа с подтверждением, 404 на чужой заказ.
- Незакрытого из 3.4 нет.

**Где грабли**

- **`next start` не переживает пересборку.** Оставшийся с прошлой подсессии `next start` после `npm run build` начал отдавать HTML со ссылками на исчезнувшие чанки: страница грузилась, но не гидратировалась, и форма входа отправлялась как обычный GET. Выглядит как ошибка в коде, а на деле — устаревший сервер. После каждой сборки процесс перезапускать. Оба сервера проверки (`node dist/main.js` и `next start -p 3000`) остаются запущенными после этой подсессии.
- **Radix Select внутри `<form>` рендерит скрытое поле.** Без пропа `name` у него нет имени, и Chrome ругается в консоли. Для каждого нового Select в форме — задавать `name`.
- **`Set-Content -Encoding utf8` в Windows PowerShell 5.1 дописывает BOM.** Правка импортов в четырёх файлах превратилась в изменение первой строки каждого. Массовые замены делать через `[System.IO.File]::WriteAllText` с `UTF8Encoding($false)` — или обычным Edit.
- **Файлы `.env` агенту читать нельзя** (запрет доступа), поэтому значения окружения проверяются только косвенно — через `env.example` и поведение приложения.
- **Ограничение в 10 файлов — на запрос, а не на заказ.** Константа называется `MAX_FILES_PER_REQUEST`, и в Фазе 4 компания сможет добавить ещё столько же в своей сдаче. Если понадобится потолок на заказ целиком — это отдельная проверка в `FilesService`, её сейчас нет.

---

## Подфаза 3.5 — frontend: детали заказа — 3 сентября 2026

**Сделано**

- **`GET /documents/:id/download`** (`backend/src/modules/files/documents.controller.ts`) — signed URL на скачивание с проверкой участия в заказе. Маршрут из ТЗ §5; `PLAN.md` относил его к 6.1, но DoD 3.5 требует скачивания, а заводить ради этого маршрут не из ТЗ было бы хуже. Список `GET /documents` остаётся за 6.1 и ляжет на этот же контроллер. Идентификатор разбирается `ParseUUIDPipe` с ответом 404: колонка `OrderFile.id` типа `uuid`, и мусор в пути упал бы в Postgres, то есть на 500.
- **`/orders/[id]`** (`frontend/src/app/(app)/orders/[id]/page.tsx` + `components/orders/order-detail.tsx`) — серверный компонент. Слева: описание работ, файлы клиента со скачиванием, размеченные блоки под Фазу 4 («Предложения компаний», «Сдачи работ»). Справа: «Объект» (категория, тип, площадь, адрес) и «Бюджет и сроки» (бюджет клиента, цена сделки, желаемая дата начала, срок, подрядчик). В заголовке — название, `ORD-номер`, дата создания и badge статуса.
- **Уточнённая площадь** показывается обоими значениями сразу: «186,5 м² — уточнено исполнителем» и «180 м² — указано клиентом» (ТЗ §4.1). Исходное значение клиента не подменяется.
- **`components/orders/delete-order-dialog.tsx`** — удаление с подтверждением. Кнопка видна только владельцу и только в статусах из `DELETABLE_ORDER_STATUSES`; после удаления — тост, переход на `/orders` и `router.refresh()`.
- **`components/orders/download-file-button.tsx`** — запрашивает ссылку в момент нажатия и переходит по ней. Подпись живёт пять минут и выдаётся под конкретного пользователя, поэтому в разметке страницы ей делать нечего.
- **`app/(app)/orders/[id]/not-found.tsx`** — «Заказ не найден» внутри каркаса кабинета. Страница отвечает настоящим 404 (проверено по коду ответа документа).
- **Хлебные крошки:** сегмент-UUID читается как «Заказ». Для компании крошка «Все заказы» больше не ссылка: раздела `/orders` у неё нет, и `proxy.ts` увёл бы её на ленту. `AppHeader` передаёт роль в `Breadcrumbs`.
- **Общее уехало в `shared/`:** `DELETABLE_ORDER_STATUSES` + `canDeleteOrder` (`shared/src/orders.ts`), тип `DownloadLink` (`shared/src/types.ts`), а весь модуль `order-participation.ts` из backend стал `shared/src/offers.ts` (`EXECUTOR_OFFER_STATUSES`, `ACTIVE_OFFER_STATUSES`, `isExecutorOffer`, `isActiveOffer`). Backend переведён на них, дубликатов не осталось.
- **`formatFileSize` переехал** из `lib/order-form.ts` в `lib/format.ts` (там же новый `formatArea`): он нужен и форме создания, и карточке заказа, а `order-form.ts` — про правила формы. `PageHeader.description` стал `ReactNode` (badge рядом с текстом), `ComingSoon` получил необязательный `title` — блоку-заглушке среди готовых карточек без заголовка непонятно, чьё это место.
- **Попутно исправлено:** у поля поиска в списке заказов не было `id`/`name` — Chrome писал замечание к разметке формы во вкладке Issues. Пришло из 3.3, чинится одной строкой.
- **Проверено:** 155 unit-тестов, 67 e2e (было 60; новые — 7 тестов на `GET /documents/:id/download` в `orders.e2e-spec.ts`: владелец, исполнитель, посторонняя компания, чужой клиент, несуществующий файл, не-UUID, без токена), `npm run typecheck`, `npm run lint`, `npm run build` всего репозитория — зелёное.
- **Проверено в браузере** (Chrome DevTools MCP, собранные backend и frontend, два аккаунта в изолированных контекстах):
  - **Сквозной DoD Фазы 3 глазами клиента:** создал заказ `ORD-153` с файлом → нашёл поиском `ORD-153` («Показано 1 из 1») → открыл детали из списка → скачал файл (`GET /documents/:id/download` → 200, затем signed URL → 200, страница не ушла) → удалил через диалог → вернулся на список, тост «Заказ удалён», база снова 6 seed-заказов.
  - Отдельно тем же путём прогнан заказ `ORD-139` с двумя файлами (PNG и PDF); после удаления папка заказа в бакете пуста — проверено временным скриптом на Storage API, обе папки вернули пустой список. Скрипт удалён.
  - `ORD-21` (в работе): показаны обе площади, цена сделки, срок, подрядчик; кнопки «Удалить» нет.
  - Компания-исполнитель (`ООО «СтройГрад»`) на `ORD-21` видит заказ целиком с файлами и без кнопки удаления; на `ORD-19`, где её предложения нет, — статус «Поиск исполнителя» и текст «Файлы задания видны компании, чьё предложение принято».
  - Удалённый заказ и мусорный идентификатор в адресе дают страницу «Заказ не найден» с кодом ответа 404.
  - Ширина 390 px (эмуляция устройства) — одна колонка, горизонтальной прокрутки нет; 1440 px — раскладка 2/3 + 1/3. Диалог удаления читается на обеих ширинах.
  - Консоль на страницах заказа и списка чистая.

**Принятые решения**

- **Маршрут скачивания взят из Фазы 6, а не придуман свой.** ТЗ §5 знает ровно один способ скачать файл — `GET /documents/:id/download`. Добавить в 3.5 что-то вроде `/orders/:id/files/:fileId` значило бы завести второй маршрут к тому же действию и потом его убирать.
- **Ссылку запрашивает браузер, а не сервер при рендере.** Простая `<a href>` не подошла бы: маршрут требует заголовок `Authorization`, а подпись Supabase живёт пять минут — вложенная в HTML, она протухла бы у любого, кто задержался на странице.
- **`403`, а не `404`, на чужой файл — оставлено как в 3.1.** Для заказов выбран 404 (существование чужого заказа не подтверждаем), но идентификатор файла нигде не показывается и не перебирается, а менять поведение сервиса, покрытого тестами с Фазы 3.1, ради единообразия формулировки — работа не этой подфазы.
- **Файлы компании на страницу пока не выводятся.** Показываются только файлы с `ownerType = CLIENT`; сдачи компании версионируются по раундам и живут в блоке «История сдач» (ТЗ §4.1), который делает 4.5. Иначе задание клиента и сдачи смешались бы в одном списке.
- **«Файлов нет» и «файлы не показаны» — разные тексты.** API в обоих случаях отдаёт пустой список, поэтому страница сама считает, сторона ли смотрящий сделки: владелец либо компания с предложением в статусе исполнителя. Ради этого `EXECUTOR_OFFER_STATUSES` и переехали в `shared/` — второй список тех же статусов на фронте разошёлся бы с backend.
- **`correctionComment` и `clientCompletionComment` на странице не показываются.** Поля в ответе есть, но без кнопок «Отправить на доработку» и «Подтвердить выполнение» комментарий висел бы в воздухе. Их место — блок приёмки в 4.4.
- **Номер заказа в крошках не показывается.** Крошки живут в шапке кабинета, то есть в layout'е, а данные заказа читает страница — layout к ним доступа не имеет. Номер виден в заголовке страницы, крошка говорит «Заказ».
- **Роль смотрящего берётся из claim'ов сессии, а не запросом профиля.** `getSessionClaims()` уже прочитан layout'ом за тот же рендер (`cache`), лишнего похода в API нет.

**Что дальше**

- **Фаза 3 завершена целиком.** Следующая — подфаза **4.1**: backend ленты доступных заказов и предложений (`GET /company/orders/available`, `GET /company/offers`, `POST /offers`, отзыв и отклонение предложения).
- Незакрытого из 3.5 нет.

**Где грабли**

- **`window.location.href` на signed URL безопасен только пока объект в бакете есть.** Supabase отдаёт его с `Content-Disposition: attachment`, и браузер сохраняет файл, не уходя со страницы. Но если строка `OrderFile` есть, а объекта нет (ровно случай seed-данных), тот же переход уведёт вкладку на JSON с ошибкой. На seed-файлах кнопку «Скачать» лучше не нажимать, пока seed не начнёт грузить настоящие файлы.
- **Окно Chrome не сжимается уже примерно до 500 px.** `resize_page` на 390 молча оставляет 500, и проверка мобильной ширины оказывается фикцией. Мерить только через эмуляцию устройства (`emulate` с `viewport: 390x844x3,mobile,touch`) и сверять `window.innerWidth`.
- **Полноэкранный скриншот врёт про липкую шапку.** При `position: sticky` + `backdrop-filter` заголовок страницы на снимке выглядит перечёркнутым размытой полосой. Проверять снимком видимой области и `getBoundingClientRect`, а не полной страницей.
- **Предупреждение в консоли про `_next/image?...&w=32` остаётся.** Логотип (`components/brand/logo.tsx`, Фаза 0) объявлен `priority`, из-за чего предзагружается вариант `w=32`, а `className="h-8 w-auto"` на экране с DPR > 1 заставляет браузер взять из srcset другой. Ни на что не влияет, но висит на каждой странице кабинета — место для 7.2, трогать чужой рабочий код посреди этой подфазы не стал.
- **`node dist/main.js` и `next start` после `npm run build` надо перезапускать оба** (грабли 3.4 в силе, теперь и для backend: старый процесс держит прежний `dist`).

---

## Ревизия по `audit.md` — находки Фазы 0 — 3 сентября 2026

Не подфаза плана: разбор находок независимого аудита (`audit.md`). Взят ровно один раздел — «Фаза 0». Находки Фаз 1–3, тестов и «вне фаз» не трогались.

**Сделано**

- **0-С1 + 0-С2 — тёмная тема заработала.** Токены `.dark` в `globals.css` лежали мёртвыми: класс `dark` никто не выставлял.
  - `components/theme-provider.tsx` — обёртка над `next-themes` (`attribute="class"`, `defaultTheme="system"`, `enableSystem`, `disableTransitionOnChange`), подключена в корневом `app/layout.tsx`. На `<html>` добавлен `suppressHydrationWarning`: класс ставит скрипт провайдера до гидратации.
  - `components/theme-toggle.tsx` — переключатель на `ui/dropdown-menu`: «Светлая» / «Тёмная» / «Как в системе». Стоит в шапке кабинета, в шапке лендинга и в углу экранов входа — то есть на всех страницах приложения.
  - `ui/sonner.tsx` не менялся: с появлением провайдера его `useTheme()` начал возвращать выбор пользователя, и тосты перестали быть тёмными на светлом интерфейсе. Проверено отдельно (см. ниже).
- **0-Н1 — `lib/types.ts` перестал быть мёртвым.** Все 20 файлов фронта, которые импортировали `@mybuild/shared` напрямую, переведены на `@/lib/types`. Сам реэкспорт — единственное место, знающее имя пакета (ТЗ §7 объявляет этот файл именно так).
- **0-Н3 — неиспользуемые компоненты дизайн-системы.** `ui/dropdown-menu` ушёл в переключатель темы, `ui/separator` — в шапку кабинета (разделитель перед городом вместо `border-l`), `ui/badge` — в `status-badge.tsx`: свой `<span>` заменён на `Badge` с переопределением цветов статуса. `ui/tabs.tsx` оставлен неиспользуемым осознанно — см. «принятые решения».
- **0-Н4 — размер логотипа.** Проп `size` был числом и управлял только `width`/`height` у `next/image`, а высоту на экране жёстко задавал `h-8`: `size={40}` и `size={24}` выглядели одинаково. Теперь `size` — это `"sm" | "md" | "lg"`, каждый шаг задаёт и размер картинки, и высоту на экране, и кегль текста. Заодно ушло предупреждение в консоли про `_next/image?...&w=32` (грабли 3.5): интринсик и CSS-высота совпали.
- **0-Н5 — `ui/dialog.tsx`**: английская подпись `Close` на кнопке в `DialogFooter` заменена на «Закрыть» (ТЗ §1).
- **0-Н6 — `frontend/README.md`** переписан по проекту: запуск, переменные окружения, защита маршрутов, тема, структура каталогов, проверки. Шаблон `create-next-app` удалён.
- **0-Н7 — `npm run typecheck` в корне** теперь начинается с `-w shared`: пакет проверялся типами только косвенно, при сборке.
- **0-Н2 (`lib/socket.ts` без импортёров) не трогал** — файл ждёт Фазы 5, это не дефект.

**Проверено**

- `npm run typecheck` (теперь включая `shared/`), `npm run lint`, `npm run build`, `npm test` (155 unit) — зелёное. Backend не менялся, e2e не гонял.
- **В браузере** (Chrome DevTools MCP, собранные backend и frontend, seed-клиент Анна Смирнова):
  - Кабинет, страница заказа и диалог удаления в тёмной теме читаются, цвета статусов различимы; выбор сохраняется между переходами и перезагрузкой (`localStorage.theme`, класс `dark` и `color-scheme` на `<html>`).
  - Режим «как в системе» реагирует на смену схемы ОС вживую (эмуляция `prefers-color-scheme`).
  - Лендинг и экран входа проверены в обеих темах, переключатель на месте на каждом.
  - **0-С2 проверен прицельно:** тема «светлая» при тёмной ОС, ошибка сети на кнопке «Скачать» → тост отрисован светлым (`data-sonner-theme=light`), а не тёмным.
  - Логотип: в шапке кабинета `md`, в подвале лендинга `sm` — размеры теперь видимо разные; консоль на всех проверенных страницах пустая (предупреждения про `_next/image` больше нет).
  - Ширина 390 px (эмуляция устройства): шапка кабинета вмещает бургер, крошки, переключатель темы и колокольчик; город с разделителем скрываются, как и раньше.
- Всё поднятое закрыто: порты 3000 и 4000 свободны, портативный Chrome закрыт, временных файлов не осталось. Выбор темы в профиле браузера сброшен обратно на «системную».

**Принятые решения**

- **`ui/tabs.tsx` оставлен неиспользуемым — решение пользователя от 3 сентября 2026.** Я предлагал удалить (файл мёртвый, возвращается командой `npx shadcn add tabs`), пользователь решил оставить. Почему он не используется: вкладки статусов — ссылки, потому что выборка обязана жить в адресе (решение 3.3), а Radix Tabs хранит выбор в состоянии; навесить `role="tab"` на навигационные ссылки значило бы сломать клавиатурное поведение рабочего экрана. На сборку файл не влияет — не импортируется, значит не собирается.
- **`audit.md` не редактировался.** Это отчёт ревизии; закрытые находки перечислены в новом `audit_fix.md` (по требованию пользователя) и здесь — по идентификаторам, как и предлагает сам файл.
- **Переключатель темы — выпадающее меню с тремя вариантами, а не кнопка-тумблер.** Тумблер не различает «светлую» и «как в системе», и пользователь не может вернуться к системной после первого клика.
- **Иконка на кнопке переключателя меняется классом (`dark:hidden` / `dark:block`), а не состоянием.** На сервере выбранная тема неизвестна, и рендер по состоянию давал бы мигание в первом кадре.
- **`StatusBadge` теперь на `ui/badge`.** Иконка стала 12 px вместо 14 (у `Badge` это задано `!important`), остальное — как было; отступы и скругление переопределены классами.

**Что дальше**

- Из находок `audit.md` закрыт раздел «Фаза 0» целиком (кроме сознательно оставленных 0-Н2 и `ui/tabs.tsx` из 0-Н3). Остаются разделы Фаз 1, 2, 3, «Тесты» и «Проблемы вне фаз» — в том числе критическая **3-К1** и три находки, помеченные аудитом как «решить до Фазы 4» (**1-С2**, **1-С3**, **3-С2**).
- Работа по плану (подфаза **4.1**) не начиналась.

**Где грабли**

- **Тост появляется в DOM только вместе с первым сообщением.** До этого `[data-sonner-toaster]` в разметке нет, и проверить тему тостов «просто посмотрев в DOM» нельзя — нужен настоящий тост (проще всего вызвать ошибку сети на кнопке «Скачать» при эмуляции Offline).
- **Любой новый цвет теперь обязан быть токеном.** С появлением реальной тёмной темы захардкоженный `bg-white` или `text-gray-900` сломает экран, а не просто «будет некрасиво». Сейчас таких мест в `src/` нет — проверено поиском.
- **`@mybuild/shared` во фронте больше не импортируется напрямую.** Новый код должен брать типы из `@/lib/types`, иначе снова разъедется с ТЗ §7. Backend по-прежнему импортирует пакет напрямую — там своей точки входа нет и не требуется.
- **Размер логотипа задаётся шагом, а не числом.** Новый вызов `<Logo size={40} />` не соберётся; варианты — `sm` / `md` / `lg`, и добавлять четвёртый нужно вместе с классами высоты и кегля, иначе картинка и текст снова разъедутся.

---

## Ревизия по `audit.md` — находки Фазы 1 — 3 сентября 2026

Не подфаза плана: разбор находок независимого аудита (`audit.md`). Взят ровно один раздел — «Фаза 1». Находки Фаз 2–3, тестов и «вне фаз» не трогались.

**Сделано**

- **1-С1 — дубликат списка статусов исполнителя убран.** `EXECUTING_OFFER_STATUSES` жил копией в `order-transition.service.ts` и отличался от `EXECUTOR_OFFER_STATUSES` из `shared/` отсутствием `COMPLETED` — намеренно, но нигде это не было выражено, и новый статус разъехался бы молча. Теперь в `shared/src/offers.ts` объявлен самый узкий список (`EXECUTING_OFFER_STATUSES` — компания работает по заказу прямо сейчас), а остальные выведены из него: `EXECUTOR_OFFER_STATUSES = [...EXECUTING, COMPLETED]`, `ACTIVE_OFFER_STATUSES = [SENT, ...EXECUTOR]`.
- **1-С2 — проигравшие предложения перестали быть безмолвными.** Эффект `DECLINE_OTHER_OFFERS` удалён целиком. `OFFER_ACCEPTED` теперь несёт `otherOffers: RivalOffer[]` (чужие предложения в `SENT` с их `companyId`), которые сервис читает под уже взятой блокировкой строки заказа. На каждое машина выдаёт `SET_OFFER_STATUS → NOT_ACCEPTED` и `CREATE_NOTIFICATION` типа `OFFER_REJECTED` — «клиент выбрал другое предложение». Эффект `SET_OFFER_STATUS` получил `companyId` (адресат, а не поле для записи), `AppliedTransition` — поле `offerUpdates: { offerId, companyId, status }[]`, из которого Фаза 5 соберёт `offer:status_changed` (ТЗ §8).
- **1-С3 — переход по предложению проверяет его статус.** В `OrderEvent` добавлен `offerStatus`, в машину — таблица `OFFER_PRECONDITIONS` (из каких статусов предложения событие имеет смысл) и ошибка `InvalidOfferStatusError` (409, `error: "InvalidOfferStatus"`). Статус заказа проверяется первым, статус предложения — вторым.
- **1-Н1 — не-UUID больше не доходит до Postgres.** Проверка вынесена в `backend/src/common/uuid.ts` (`isUuid`); `OwnershipGuard` переведён на общий хелпер, `OrderTransitionService` проверяет и `orderId` (уходил в сырой `::uuid`), и `offerId` (уходил в Prisma на колонку `uuid`) — оба давали 500 вместо 404.
- **1-Н2 — ключи файлов в seed стали настоящими.** `buildStorageKey` переехал из `files.service.ts` в чистый `file-validation.ts` и вызывается обоими. Заказ в seed теперь создаётся первым, а файлы — отдельным `createMany`: в ключе нужен `orderId`, а при вложенном `create` его ещё нет.
- **1-Н3 — уведомление проигравшей компании в seed** получило тип `OFFER_REJECTED`, заголовок «Предложение отклонено» и тот же текст, что выдаёт машина.
- **1-Н4 — `EXECUTOR_OFFER_STATUSES` и соседи объявлены `readonly`.** Prisma ждёт изменяемый массив, поэтому в запросах идёт копия (`[...СПИСОК]`); в `orders.service.ts` копия сделана один раз константой, а не в каждом запросе.
- **1-Н5 (`OrderFile.uploadedById`) не добавлял** — решение пользователя, см. ниже.

**Проверено**

- `npm run typecheck` (shared + backend + frontend), `npm run lint` (ноль замечаний), `npm run build` всего репозитория — зелёное.
- `npm test` — **163 unit** (было 155). Новые: проигравшее предложение получает и статус, и уведомление; принятие без соперников не трогает чужие статусы; повторное отклонение отклонённого, принятие отозванного и вторая сдача уже сданной работы падают с `InvalidOfferStatusError`; повторная отправка предложения после отказа клиента разрешена; статус заказа проверяется раньше статуса предложения.
- `npm run test:e2e` — **69 e2e** (было 67) на живой Supabase. Новые: повторное отклонение предложения даёт 409 и не создаёт второго уведомления; заказ с не-UUID идентификатором даёт 404. В тест полного цикла добавлена проверка уведомления проигравшей компании и состава `offerUpdates`.
- `npm run db:seed` прогнан на живой базе (4 пользователя, 6 заказов, 8 предложений, 12 файлов, 8 уведомлений). Временным скриптом сверено: ключи вида `orders/{orderId}/client/0/{хеш16}-{имя}`, уведомление проигравшей компании с типом `OFFER_REJECTED`. Скрипт удалён.
- Ничего не осталось поднятым: серверы не запускались, браузер не открывался — правки backend'а и seed, экраны не менялись.

**Принятые решения**

- **Проверка статуса предложения — в машине, а не в выборке сервиса.** Можно было отфильтровать `resolveOffer` по допустимым статусам, но тогда «нельзя отклонить дважды» стало бы 404 «предложение не найдено» и жило бы вне state-машины, вопреки `CLAUDE.md` §5. Сейчас это правило перехода: таблица рядом с таблицей переходов, проверяется unit-тестами без базы, наружу идёт честный 409.
- **`OFFER_SUBMITTED` разрешён из `SENT / WITHDRAWN / REJECTED / NOT_ACCEPTED`.** У `Offer` есть `@@unique([orderId, companyId])`, то есть повторное предложение — это обновление существующей строки. Запрещены только исполнительские статусы: работающая по заказу компания не может «прислать предложение заново».
- **Проигравшие перечисляются поимённо, а не одним `updateMany`.** Уведомление нужно каждой компании, а Фазе 5 — список адресатов. Побочный выигрыш: список читается под блокировкой строки заказа, то есть параллельный переход его не изменит.
- **Обновления статусов сгруппированы по значению статуса.** Один `updateMany` на статус: сколько бы предложений ни было у заказа, запросов остаётся не больше двух. Группы не пересекаются по id, поэтому идут через `Promise.all`.
- **1-Н5 — `uploadedById` не добавляем. Решение пользователя от 3 сентября 2026.** Я рекомендовал оставить как есть: `ownerType` уже отвечает на вопрос «чей файл», исполнитель у заказа один, а колонка стала бы вторым источником правды рядом с офферами — ценой миграции и правок seed и e2e.
- **`audit.md` не редактировался** (как и в прошлый раз): закрытые находки перечислены в `audit_fix.md` и здесь, по идентификаторам.

**Что дальше**

- Из `audit.md` закрыты разделы «Фаза 0» и «Фаза 1». Остаются Фазы 2 и 3, «Тесты» и «Проблемы вне фаз» — в том числе критическая **3-К1** (файлы целиком в памяти процесса) и **3-С2** (гонка при удалении заказа), которую аудит просит закрыть до Фазы 4.
- Работа по плану (подфаза **4.1**) не начиналась.

**Где грабли**

- **Фаза 4 обязана передавать в переход настоящий статус предложения.** `OrderTransitionService` читает его сам, но если появится второй путь применения перехода — событие без верного `offerStatus` либо упадёт с 409, либо пропустит проверку.
- **Создание предложения и переход `OFFER_SUBMITTED` должны идти одной транзакцией.** Список проигравших читается под блокировкой строки заказа, но предложение, созданное *вне* перехода в момент принятия чужого, в этот список не попадёт и останется висеть в `SENT`. С прежним `updateMany` дыра была ровно такой же — она не новая, но в 4.1 её надо закрыть, а не унаследовать.
- **Списки статусов в `shared/` теперь `readonly`.** В запрос Prisma их надо копировать (`[...СПИСОК]`), иначе типы не сойдутся: `EnumOfferStatusFilter.in` объявлен изменяемым массивом.
- **`buildStorageKey` живёт в `file-validation.ts`, а не в сервисе.** Причина — seed: тянуть в скрипт `FilesService` с базой и хранилищем ради одной строки нельзя (`tsx` не поднимает Nest, грабли 3.1).
- **Seed заново создаёт заказы при каждом прогоне**, поэтому их номера и идентификаторы меняются. Ссылки на конкретный `ORD-…` из прежних записей журнала после `npm run db:seed` недействительны.

---

## Ревизия по `audit.md` — находки Фазы 2 — 3 сентября 2026

Не подфаза плана: разбор находок независимого аудита (`audit.md`). Взят ровно один раздел — «Фаза 2». Находки Фазы 3, тестов и «вне фаз» не трогались.

**Сделано**

- **2-С1 — `/health` перестал рассказывать о базе.** Поле `databaseError` убрано из ответа и из типа `HealthResponse`: в сообщении Prisma/pg бывают хост, порт и имя базы, а маршрут открыт без токена. Причина теперь уходит в лог контроллера. Заодно на маршрут повешен `ThrottleGuard` с лимитом 60/мин: он делает настоящий `SELECT 1`, но по нему же будет ходить внешний пингер из `CLAUDE.md` §7.1, поэтому лимит щедрый.
- **2-С2 — `SupabaseJwtService` покрыт тестами.** `supabase-jwt.service.spec.ts`: настоящая пара ключей ES256, настоящая `jose`, подменяется только `fetch` за JWKS. 21 тест — правильный токен, подпись чужим ключом, подделанная нагрузка, чужой `iss`, чужой `aud`, истёкший срок, мусор вместо токена, отсутствие `sub`, кэш ключей (второй `verify` в сеть не ходит), разбор `user_role` и `email_verified`, единый текст отказа наружу.
- **2-С3 — подтверждение email проверяется кодом.** Миграция `20260903170000_email_verified_claim` заменяет `custom_access_token_hook`: он кладёт в токен claim `email_verified` из `auth.users.email_confirmed_at` (и по-прежнему `user_role`). `AuthUser` получил поле `emailVerified`, `SupabaseAuthGuard` отвечает 403 «Подтвердите email», `proxy.ts` уводит такого пользователя на новый экран `/verify-email`.
- **2-С4 + 2-Н1 — сессия без роли перестала вести в пустоту.** Появился экран `/no-role` с прямым объяснением (не включён Custom Access Token Hook) и кнопкой «Выйти»; `getHomeHref(null)` ведёт туда же. В `proxy.ts` убрано `if (role && …)`: отсутствие роли больше не означает «пускать всюду».
- **2-Н2 — формат телефона.** `shared/src/profile.ts`: `PHONE_PATTERN` (необязательный `+`, цифры, пробелы, скобки, дефисы; всего 10–15 цифр), `PHONE_MAX_LENGTH`, `PHONE_ERROR_MESSAGE`, `isValidPhone`. Проверяют одно и то же форма регистрации и DTO `PATCH /profile`.
- **2-Н3 — `PATCH /profile`** под `ThrottleGuard`, 20 запросов в минуту (ТЗ §6).
- **2-Н4 — `frontend/env.example`** называет `frontend/.env` (проект использует именно его), `.env.local` упомянут как место личных переопределений.
- **2-Н5 — `decodeURIComponent`** в крошках обёрнут в `try/catch`.
- **2-Н6 — крошка «Главная»** ведёт в кабинет роли, а не на `/`, откуда вошедшего немедленно перекидывало обратно. Когда первый сегмент пути и есть кабинет (`/orders` у клиента, `/available` у компании), крошка не показывается — иначе две соседние ссылки вели бы в одно место.

**Проверено**

- `npm run typecheck` (shared + backend + frontend), `npm run lint`, `npm run build` — зелёное, 20 маршрутов Next (было 18).
- `npm test` — **186 unit** (было 163). `npm run test:e2e` — **77 e2e** (было 69): 2 на хук доступа, 5 на формат телефона, 1 на то, что `/health` не отдаёт подробностей.
- **Миграция применена к живой базе** (`npm run db:deploy`) и сверена: вход seed-клиента даёт токен с `user_role: CLIENT` и `email_verified: true`; вызов хука для учётной записи, созданной с `email_confirm: false`, даёт `email_verified: false` — это и проверяет новый e2e.
- `/health` вручную: ответ `{status, uptimeSeconds, database}` без текста ошибки, 429 после 60 запросов в минуту.
- **В браузере** (Chrome DevTools MCP, `npm run dev`): вход клиентом и компанией; крошки на `/orders` («Все заказы»), `/settings` («Главная → /orders / Настройки»), `/documents` у компании («Главная → /available / Документы») и на странице заказа у обеих ролей; здоровый пользователь с `/no-role` и `/verify-email` уводится в кабинет, вышедший — на `/login`; оба служебных экрана отрисованы и кнопка «Выйти» на них работает; форма регистрации отклоняет телефон «телефон» с русским текстом ошибки и не создаёт учётной записи.
- Всё поднятое закрыто: порты 3000 и 4000 свободны, Chrome закрыт, временные скрипты удалены.

**Принятые решения**

- **Источник правды о подтверждении — хук, а не `user_metadata`. Вариант выбран пользователем 3 сентября 2026** из трёх предложенных. Причина: `user_metadata.email_verified` пользователь переписывает сам через `updateUser`, то есть проверка обходилась бы одним запросом. `auth.users.email_confirmed_at` пишет только GoTrue.
- **Нет claim'а `email_verified` — считаем подтверждённым.** Так рассуждают и backend, и фронт. Отсутствие claim'а означает, что хук выключен, — но тогда нет и роли, а без роли кабинет всё равно закрыт и человек видит `/no-role` с объяснением. Обратный выбор превратил бы выключенный хук в полностью неработающее приложение без внятной причины.
- **Проверка подтверждения — в `SupabaseAuthGuard`, а не в `RolesGuard`.** Она не про роль, а про право пользоваться API вообще; в guard'е аутентификации её нельзя забыть на новом маршруте.
- **Два служебных экрана, а не один с параметром.** Тексты разные («подтвердите адрес» и «включите хук»), адреса читаемые, разбора query не требуется.
- **`/callback` и `/reset-password` исключены из увода на служебные экраны.** Ссылка подтверждения из письма ведёт именно на `/callback`; увод оттуда означал бы, что подтвердить адрес нельзя никогда.
- **Ограничение частоты на `/health` — 60/мин на адрес, а не отказ от ограничения.** Маршрут открыт и делает запрос к базе; при этом лимит обязан пропускать внешний пингер и ручные проверки.
- **`audit.md` не редактировался** (как и в прошлые два раза): закрытые находки перечислены в `audit_fix.md` и здесь, по идентификаторам.

**Что дальше**

- Из `audit.md` закрыты разделы «Фаза 0», «Фаза 1» и «Фаза 2». Остаются «Фаза 3», «Тесты» и «Проблемы вне фаз» — в том числе критическая **3-К1** (файлы целиком в памяти процесса) и **3-С2** (гонка при удалении заказа), которую аудит просит закрыть до Фазы 4. Находки **Т-С1** больше нет: это дубль 2-С2.
- Работа по плану (подфаза **4.1**) не начиналась.

**Где грабли**

- **Новый claim появляется только в новом токене.** У того, кто вошёл до применения миграции, `email_verified` в токене нет до обновления access-токена (час или ближайший refresh). На поведение это не влияет только потому, что отсутствие claim'а трактуется как «подтверждён»; если когда-нибудь решим трактовать наоборот — сначала дождаться ротации токенов.
- **Хук нельзя ронять исключением.** Если `custom_access_token_hook` бросит ошибку, GoTrue не выдаст токен вообще и войти не сможет никто. Поэтому обе выборки в нём мягкие: нет профиля — нет claim'а роли, нет учётной записи — `email_verified: false`.
- **`prisma migrate deploy` работает только без песочницы агента** (грабли Фазы 1 в силе): в песочнице schema-engine падает с `P1001`.
- **Тест JWKS: подмена `fetch` обязана возвращать Promise.** `jose` вешает `.catch` прямо на результат вызова, и синхронно возвращённый `Response` даёт «signature verification failed» вместо внятной ошибки — полчаса на поиск.
- **Невалидную процентную последовательность в адресе Next отбивает раньше нас** (400 Bad Request на `/orders/%C3%28`), поэтому падение из 2-Н5 через браузер не воспроизводится. `try/catch` в крошках оставлен как страховка на клиентскую навигацию (`usePathname` получает путь как есть).
- **В dev-режиме на логотипе снова висит предупреждение `next/image`** про изменённую только одну сторону. В прод-сборке его нет (проверялось в прошлый сеанс), трогать чужой рабочий код посреди этой правки не стал — место для 7.2.

---

## Ревизия по `audit.md` — находки Фазы 3 — 4 сентября 2026

Не подфаза плана: разбор находок независимого аудита (`audit.md`). Взят ровно один раздел — «Фаза 3». Находки из «Тестов» и «вне фаз» не трогались. Полный список закрытого — в `audit_fix.md`.

**Сделано**

- **3-К1 (критическая) — файлы больше не лежат в памяти процесса.** Вариант выбран пользователем: диск и обработка по одному, лимиты ТЗ (20 МБ × 10) сохранены.
  - `FilesInterceptor` получил `dest: UPLOAD_TEMP_DIR`; `UploadedFileInput` вместо `buffer` несёт `path` и `sizeBytes`.
  - Новый `src/modules/files/uploaded-file.ts` — единственное место модуля, ходящее в файловую систему: `prepareFile` (проверки + потоковый SHA-256 + чтение первых байт), `readFileBuffer`, `removeTempFiles`, `UPLOAD_TEMP_DIR`. `file-validation.ts` остался чистым.
  - Загрузка в бакет — строго по одному файлу, под общим на процесс семафором (`src/common/semaphore.ts`, 4 слота). Пик памяти ~80 МБ вместо ~1 ГБ.
  - `src/common/guards/upload-size.guard.ts` — отказ 413 по `Content-Length` до разбора тела (`MAX_UPLOAD_REQUEST_BYTES` в `shared/`).
  - `src/common/interceptors/temp-upload-cleanup.interceptor.ts` — уборка временных файлов после любого ответа; стоит **первым**, поэтому охватывает и отказ валидации DTO.
- **3-С1 — уборка осиротевших объектов переписана.** При гонке ключ чужой строки совпадает с нашим (в нём хеш содержимого), и прежнее «удалить несохранённые ключи» стирало объект живой строки. Теперь сервис спрашивает базу, какие ключи сдачи в ходу, и убирает только те, на которые не ссылается никто.
- **3-С2 — удаление заказа стало условным:** `deleteMany({ where: { id, status: { in: DELETABLE_ORDER_STATUSES } } })`, ноль строк → 409. Проверка по снимку guard'а осталась как быстрый путь с понятным сообщением.
- **3-С3 — появились границы ошибок:** `components/error-state.tsx` + `app/(app)/error.tsx`, `app/error.tsx`, `app/global-error.tsx`.
- **3-С4 — заказчик виден только сторонам сделки** (решение пользователя): `client` в `OrderDetail` стал `… | null`.
- **3-Н1** — `escapeLike` в `order-search.ts`. **3-Н3** — `assertFileSignature` (magic bytes для PDF/PNG/JPEG/WEBP/DWG). **3-Н4** — файлы проверяются до создания строки заказа (`prepareUploads`). **3-Н6** — `formatMoney` считает по строке. **3-Н7/3-Н8** — запятая в бюджете, `replaceAll`. **3-Н9** — комментарий про часовые пояса исправлен. **3-Н10** — «Заказ не найден» ведёт в кабинет роли. **3-Н12** — `pageSize` передаётся явно.
- **3-Н2 (WEBP и DXF в allowlist) оставлено**, решение пользователя; **3-Н5** отложено к 7.3 как и было записано; **3-Н11** закрыто отказом — не дефект.

**Проверено**

- `npm run typecheck`, `npm run lint`, `npm run build` — зелёное. `npm test` — **212 unit** (было 186), `npm run test:e2e` — **81 e2e** (было 77).
- Новые тесты: `semaphore.spec.ts`, `upload-size.guard.spec.ts`, `uploaded-file.spec.ts`, подписи форматов в `file-validation.spec.ts`, два сценария гонки в `files.service.spec.ts`, `escapeLike` в `order-search.spec.ts`, приватность заказчика в `order-view.spec.ts`; в e2e — подделка PDF, экранирование `%` и `_`, `client: null` у посторонней компании и `client` у сторон сделки.
- **В браузере** (Chrome DevTools MCP, собранные backend и frontend, seed-клиент и seed-компания): загрузка PDF+PNG через форму и через API; `MZ` под именем `план.pdf` → 400 и заказ не создан; временный каталог пуст и после успеха, и после отказа валидации DTO; поиск `%` → 0, `Ремонт_квартиры` → 0, «Проверка» → 1; посторонняя компания получает `client: null` и страница отрисовывается; «Заказ не найден» у компании ведёт на `/available`; остановленный backend → экран «Не удалось загрузить страницу», «Повторить» возвращает список без перезагрузки; граница внутри кабинета проверена временным `throw` под `next dev` (снят); бюджет «45000,50» принят и показан как «45 000,5 USD»; пагинация прогнана на 22 заказах (20+2, кнопки блокируются на краях); удаление через диалог — 204 и тост. Консоль чистая.
- **Убрано за собой:** 22 тестовых заказа удалены, база вернулась к seed-состоянию (6 заказов), бакет пуст, временные скрипты удалены, порты 3000/3001/4000 свободны, Chrome закрыт.

**Принятые решения**

- **Диск вместо потолка на суммарный размер запроса. Решение пользователя от 4 сентября 2026** из двух вариантов. Второй (лимит ~30 МБ на весь запрос + семафор) был вдвое дешевле, но резал возможность приложить 10 файлов по 20 МБ, то есть менял продуктовое правило ради экономии на правках.
- **Заказчик скрывается целиком, а не «оставим город». Решение пользователя от 4 сентября 2026.** Половинчатый вариант ничего не давал: адрес объекта виден всем компаниям и так.
- **WEBP и DXF в allowlist остаются. Решение пользователя от 4 сентября 2026.** Отступление от буквы ТЗ §6 зафиксировано в `CLAUDE.md` §6, ТЗ не правился.
- **`@types/multer` не ставили и `multer` напрямую не импортировали.** Для диска хватило опции `dest` из `MulterOptions` — `diskStorage` потребовал бы импорта пакета без типов.
- **Уборка временных файлов — интерсептором, а не в контроллере.** До тела контроллера запрос может не дойти: `ValidationPipe` работает позже интерсепторов, и на кривом поле формы файлы уже лежали бы на диске.
- **Проверка содержимого — по таблице подписей, без библиотеки.** Пять форматов, по 6–12 байт на каждый; тянуть `file-type` незачем.
- **3-Н11 закрыта отказом.** `setPending(false)` после `window.location.href` — не ошибка: переход на вложение страницу не выгружает, а без сброса кнопка навсегда осталась бы в «Готовим…».
- **`audit.md` не редактировался** (как и в прошлые три раза): закрытые находки перечислены в `audit_fix.md` и здесь, по идентификаторам.

**Что дальше**

- Из `audit.md` закрыты все четыре раздела по фазам — 0, 1, 2 и 3. Остаются «Тесты» (**Т-С2**, **Т-С3**, **Т-С4**, **Т-Н1**…**Т-Н5**) и «Проблемы вне фаз» (**В-С1** — ни одного теста фронтенда, **В-Н1** — `.mcp.json` с путём к Chrome конкретной машины). Ни одна из них не помечена аудитом как «до Фазы 4».
- Работа по плану (подфаза **4.1**) не начиналась.

**Где грабли**

- **`UploadedFileInput` больше не несёт `buffer`.** Любой новый вызывающий (сдача работ компании в 4.5) обязан отдавать `path` и `sizeBytes`, а вызывать `attachFiles` — только после `prepareUploads`. Порядок «проверить → создать строку → загрузить» держится на этом.
- **Временные файлы удаляет интерсептор, а не сервис.** Новый маршрут с `FilesInterceptor` обязан подключить `TempUploadCleanupInterceptor` и поставить его **первым**, иначе `%TEMP%/mybuild-uploads` будет расти молча.
- **Семафор общий на процесс и живёт в модуле, а не в DI.** При нескольких экземплярах backend'а потолок памяти станет общим только внутри каждого — та же оговорка, что у `ThrottleGuard` (7.3).
- **Проверка подписи ломает тестовые файлы «просто текст».** Фикстуры собираются `pdfBytes()`/`pngBytes()` из `test/support/uploads.ts`; строка вроде `Buffer.from('%PDF-1.4 план')` в новом тесте пройдёт, а `Buffer.from('содержимое')` — уже нет.
- **`Buffer.from('\x89PNG…')` в UTF-8 даёт не тот байт** (0xC2 0x89 вместо 0x89). Прежняя фикстура PNG в `orders.e2e-spec.ts` была именно такой и с проверкой подписи не прошла бы — заменена на настоящие восемь байт.
- **Проп границы ошибок в Next 16 — `retry`, а не `reset`.** `reset` ещё существует, но только для сброса без повторного запроса; для «попробовать снова» нужен `retry`.
- **`global-error.tsx` не видит глобальных стилей.** Классы Tailwind и токены темы туда не доезжают — стили только инлайновые (это написано и в документации Next в `node_modules`).
- **Границу `(app)/error.tsx` не воспроизвести остановкой backend'а:** каркас кабинета тянет профиль тем же запросом и падает раньше, поэтому срабатывает `app/error.tsx`. Проверять только временным `throw` на странице.

---

## Ревизия по `audit.md` — раздел «Проблемы вне фаз» — 4 сентября 2026

Не подфаза плана: пятый сеанс по отчёту независимого аудита. Взят раздел «Проблемы вне фаз».
Раздел «Тесты» не трогался. Список закрытого — в `audit_fix.md`.

**Сделано**

- **В-С1 — у фронта появились тесты.** 136 unit в 11 файлах, Vitest — тот же, что у backend.
  - `frontend/vitest.config.mts` (окружение `node`, `resolve.tsconfigPaths` под алиас `@/`),
    скрипты `test` и `test:watch` в `frontend/package.json`, `vitest` в его devDependencies.
  - Корневой `npm test` теперь гоняет backend **и** frontend; добавлены `test:backend`
    и `test:frontend` на случай, когда нужен один набор.
  - Покрыто: `lib/redirects` (open redirect), `lib/orders-filter`, `lib/order-form`,
    `lib/format`, `lib/session`, `lib/navigation`, `lib/api`, `lib/auth-errors`,
    `src/proxy.ts` (защита маршрутов целиком) и две новые чистые функции.
- **Вынесены две функции из компонентов (решение пользователя от 4 сентября 2026):**
  - `lib/order-access.ts` — `resolveOrderDetailAccess` (владелец / сторона сделки / файлы
    задания) и `emptyClientFilesMessage`; `components/orders/order-detail.tsx` теперь
    только рисует.
  - `lib/breadcrumbs.ts` — `buildBreadcrumbs`; `components/layout/breadcrumbs.tsx`
    сократился до разметки. Крошка теперь описывается как `{ label, href, current }`,
    где `href: null` — «показать текстом».
  - Поведение обоих экранов не менялось — сверено в браузере.
- **В-Н1, В-Н2, В-Н3 оставлены как есть — решение пользователя от 4 сентября 2026.**
  Разработчик один и на сервере `.mcp.json` не будет; корневой README и открытые вопросы
  §7.1/§7.2 — работа подфазы 7.4.

**Проверено**

- `npm run typecheck` (shared + backend + frontend — спеки попадают в него, `tsc` уже
  поймал несуществующий `OrderCategory.RENOVATION`), `npm run lint`, `npm run build`
  (19 маршрутов) — зелёное.
- `npm test` — **212 unit backend** (не менялось) + **136 unit frontend** (было 0).
- **В браузере** (собранные backend и frontend, seed-аккаунты `anna.client@mybuild.test`
  и `info@stroygrad.mybuild.test`): крошки у клиента на `/orders` («Главная» скрыта, потому
  что это его кабинет) и на странице заказа («Все заказы / Заказ»); у компании
  на `/documents` («Главная» → `/available`) и на странице заказа («Главная / Все заказы —
  текстом / Заказ»); карточка заказа глазами владельца в `WAITING` (файлы + «Удалить заказ»),
  исполнителя ORD-183 (файлы, кнопки удаления нет) и посторонней компании на ORD-185
  («Файлы задания видны компании, чьё предложение принято»). Консоль чистая на всех экранах.
- **Убрано за собой:** оба сервера остановлены, порты 3000 и 4000 свободны, Chrome закрыт,
  временных файлов не осталось, база не менялась.

**Принятые решения**

- **Vitest, а не Jest/Playwright.** У backend уже Vitest — второй раннер в репозитории
  означал бы вторую конфигурацию, второй словарь и второй способ запуска.
- **Окружение `node`, без jsdom и Testing Library.** После выноса двух функций логики,
  которую нельзя проверить без DOM, не осталось: компоненты стали разметкой. Компонентные
  тесты (в первую очередь `OrdersSearch` с его debounce) — работа подфазы 7.2.
- **`vitest.config.mts`, а не `.ts`.** У `frontend/package.json` нет `"type": "module"`,
  и Vite грузил бы конфиг как CommonJS с предупреждением на каждый прогон.
- **Спеки лежат рядом с кодом** (`src/lib/*.spec.ts`), как в backend, а не в отдельном
  `__tests__`. Тест и модуль правятся одним движением.
- **`audit.md` не редактировался** (как и в прошлые четыре раза): закрытое перечислено
  в `audit_fix.md` по идентификаторам.

**Что дальше**

- Из `audit.md` остался единственный раздел — **«Тесты»**: **Т-С2** (`OwnershipGuard`),
  **Т-С3** (`OrdersService` и `OrderTransitionService`), **Т-С4** (`OFFER_REJECTED` в e2e)
  и **Т-Н1**…**Т-Н5**. Ни одна не помечена аудитом как «до Фазы 4».
- Работа по плану (подфаза **4.1**) не начиналась.

**Где грабли**

- **Vitest не проверяет типы.** `OrderCategory.RENOVATION` (такого значения нет) прошёл
  прогон тестов и упал только на `npm run typecheck`. После правки спеков typecheck
  обязателен, «тесты зелёные» — не то же самое.
- **`mockResolvedValue(new Response(...))` в тестах API отдаёт один и тот же объект,**
  а тело `Response` читается один раз: второй вызов падает с «Body is unusable».
  Нужен `mockImplementation(() => new Response(...))`.
- **`NEXT_PUBLIC_API_URL` читается модулем `lib/api.ts` один раз при загрузке.**
  Подменять его надо `vi.stubEnv` **до** динамического `await import("./api")`, иначе
  подмена не действует.
- **`vi.mock` в `proxy.spec.ts` указывает путь так же, как сам `proxy.ts`** —
  `"./lib/supabase/proxy"`. Через алиас `@/` это другой идентификатор модуля,
  и мок может не примениться.
- **Спеки попадают в `tsconfig` фронта, а значит и в `next build`.** Ошибка типов
  в тесте ломает сборку — это осознанно (иначе спеки жили бы без проверки типов),
  но неожиданно, если увидеть это впервые на сборке.

---

## Ревизия по `audit.md` — раздел «Тесты» — 4 сентября 2026

Не подфаза плана: шестой и последний сеанс по отчёту независимого аудита. Взят раздел
«Тесты» — **Т-С2**, **Т-С3**, **Т-С4**, **Т-Н1**…**Т-Н5**. Список закрытого —
в `audit_fix.md`. После этого сеанса непроверенных разделов `audit.md` не осталось.

**Сделано**

- **Т-С2 — `OwnershipGuard` покрыт unit-тестами.**
  `backend/src/common/guards/ownership.guard.spec.ts`, 16 тестов без сети. Декораторы
  настоящие (`@OrderAccess` на тестовом классе), режим читает тот же `Reflector`,
  что и приложение. Подставная база фильтрует предложения по `companyId` по-настоящему —
  иначе тест не отличил бы «своё предложение компании» от чужого. Отдельно проверено,
  что маршрут **без декоратора** считается закрытым и что мусорный `:id` не доходит
  до базы.
- **Т-С3 — оба сервиса заказов покрыты unit-тестами.**
  - `orders.service.spec.ts` (23 теста): порядок обращений проверяется через
    `mock.invocationCallOrder` — проверка файлов до `order.create` (правка 3-Н4),
    ключи объектов до удаления строки, бакет после. Плюс откат при сбое загрузки,
    ошибка отката не подменяет причину отказа, `list` (фильтры, `skip`/`take`,
    `totalPages`, пустой список), `getDetail`, `remove` (409 без похода в базу,
    условие статуса в `deleteMany`, 409 при нуле строк — и тогда бакет не трогаем).
  - `order-transition.service.spec.ts` (20 тестов): машина настоящая, транзакция
    подставная. `$transaction` вызывается один раз и с `timeout: 15_000`; `FOR UPDATE`
    идёт первым запросом; `SET_OFFER_STATUS` группируется по статусу (два `updateMany`
    на принятие, а не по запросу на предложение); уведомления победителю и проигравшему
    уходят одним `createManyAndReturn`; `offerUpdates` содержит адресатов для Фазы 5;
    запрещённый переход и неподходящий статус предложения не доходят до записи.
- **Т-С4 — правка не потребовалась.** `OFFER_REJECTED` уже покрыт e2e с прошлого
  сеанса (тест «отклоняет предложение один раз…» появился при исправлении 1-С3),
  вместе с уведомлением проигравшей компании. Подтверждено прогоном 81 e2e.
- **Т-Н1 — окружение health-теста больше не течёт наружу.** `Object.assign(process.env, …)`
  заменён на `vi.stubEnv` + `vi.unstubAllEnvs()` в `afterAll`. Изоляция файла перестала
  зависеть от настроек пула тестов.
- **Т-Н2 — уборка e2e стала пофайловой.** `test/support/e2e-users.ts` теперь отдаёт набор:
  `e2eSuite(name)` → `{ createUser, dropUsers, domain }`. Адреса пользователей — в своём
  поддомене (`@auth.e2e.test`, `@files.e2e.test`, `@orders.e2e.test`,
  `@transition.e2e.test`), `dropUsers()` сносит только их. Четыре файла e2e переведены
  на наборы.
- **Т-Н3 — у `shared/` появились тесты: 108 в 5 файлах.** `shared/vitest.config.mts`
  (окружение `node`, без SWC — в пакете нет декораторов), скрипты `test`/`test:watch`,
  `vitest` в devDependencies. Сборка пакета переехала на `tsconfig.build.json`,
  исключающий `*.spec.ts`. Покрыты `canDeleteOrder`, `MONEY_PATTERN`, `ORDER_LIMITS`,
  `isExecutorOffer`/`isActiveOffer` и вывод трёх списков статусов, `fileExtension`
  и таблица расширений, `parseOrderNumber`/`formatOrderNumber` вместе с границей
  `MAX_ORDER_NUMBER`, названия и цвета для всех значений enum-ов, `isValidPhone`.
  Корневой `npm test` гоняет shared → backend → frontend; добавлен `test:shared`.
- **Т-Н4 — `ConfigModule` в `order-transition.e2e-spec.ts`** получил `cache: true`
  и `validate: validateEnv`, как в `AppModule`.
- **Т-Н5 — у теста ограничителя частоты свой пользователь** (`orders-throttled`),
  который больше нигде не используется.

**Проверено**

- `npm run typecheck` (shared + backend + frontend), `npm run lint` (ноль замечаний),
  `npm run build` — зелёное.
- `npm test` — **108 unit shared** (было 0) + **271 unit backend** (было 212) +
  **136 unit frontend** (не менялось).
- `npm run test:e2e` — **81 e2e**, все прошли на живой Supabase уже после перехода
  на поддомены, `vi.stubEnv` и отдельного пользователя ограничителя.
- Учётных записей с адресом в `e2e.test` после прогона в базе не осталось — проверено
  запросом к Admin API временным скриптом, скрипт удалён.
- **Убрано за собой:** серверы не поднимались (браузер в этом сеансе не нужен —
  прикладной код не менялся), временных файлов не осталось, база вернулась
  к состоянию до прогона (5 учётных записей seed).

**Принятые решения**

- **Vitest в `shared/`, а не вынос тестов пакета в backend.** Тест должен лежать рядом
  с кодом, который проверяет, иначе правка `shared/` не тянет за собой правку теста.
  Цена — одна devDependency и один конфиг; SWC там не нужен, декораторов в пакете нет.
- **Сборка `shared/` переведена на `tsconfig.build.json`.** Спеки обязаны проверяться
  типами (`tsconfig.json` их включает), но в `dist` попадать не должны: пакет
  импортируют оба приложения, и `*.spec.js` с зависимостью на `vitest` там лишний.
- **Поддомен на файл вместо общего домена и вместо уборки по списку id.** Уборка
  по своим id не подчистила бы следы прогона, прерванного по таймауту, — а это ровно
  тот случай, ради которого уборка в `beforeAll` и существует.
- **Т-С4 закрыт без правки.** Аудит писался до сеанса по Фазе 1; нужный e2e появился
  там. Дописывать второй тест того же самого не стал.
- **`audit.md` не редактировался** (как и в прошлые пять раз): закрытое перечислено
  в `audit_fix.md` по идентификаторам.

**Что дальше**

- `audit.md` разобран целиком. Незакрытых находок не осталось; отказы (**0-Н2**,
  **1-Н5**, **3-Н2**, **3-Н5**, **3-Н11**, **В-Н1**…**В-Н3**) перечислены в `audit_fix.md`.
- Работа по плану (подфаза **4.1**) не начиналась.

**Где грабли**

- **Подставной мок без объявленных параметров ломает типы, а не тесты.**
  `vi.fn(async () => …)` даёт `mock.calls` типа `[]`, и `calls[0][0]` перестаёт
  существовать для `tsc` — прогон при этом зелёный. Параметры в моках перечисляются
  явно, даже неиспользуемые.
- **`requestFor(user, undefined)` не то же самое, что запрос без параметра:**
  значение по умолчанию у аргумента подставится, и тест проверит не то. Отсутствие
  параметра собирается объектом руками.
- **UUID в тестах должен быть настоящим v4:** `isUuid` проверяет версию и вариант,
  и `1111…-1111-…` не проходит. Отсюда `…-4111-8111-…` в константах спеков.
- **`vi.stubEnv` действует только до `unstubAllEnvs`,** а `ConfigModule` читает
  окружение в момент `app.init()`. Подмена ставится до импорта `AppModule`, снимается
  после `app.close()` — иначе приложение доживает до восстановленного окружения.
- **Поддомены `@*.e2e.test` Supabase принимает,** но домен фигурирует в трёх местах:
  адресе пользователя, фильтре уборки и (исторически) в описаниях тестов. Меняя его,
  проверять надо все три — иначе уборка молча перестанет находить своих.
