# progress.md — журнал текущей фазы

Рабочий журнал между подсессиями **одной фазы**. Правила проекта, состояние кода и грабли — `CLAUDE.md`. План подфаз — `PLAN.md`. Требования — `MyBuild_NestJS_TZ.md`.

---

## Как этим пользоваться

**В начале подсессии:**

1. Прочитать этот файл целиком — здесь только текущая фаза, это быстро.
2. Посмотреть в `PLAN.md`, какая подфаза идёт следующей.
3. Прочитать раздел ТЗ, к которому она относится.
4. Взять **ровно одну** подфазу. Не больше.

**В конце подсессии:**

1. Дописать запись по шаблону ниже — четыре блока, ни один не пропускать.
2. Дать в чат короткий отчёт и указать: «в фазе N осталось: …» либо «фаза N завершена целиком».
3. Остановиться. Не спрашивать «делать дальше?».

**Правила для записей:**

- Новая запись добавляется **вниз**. Прежние записи этой фазы не переписывать.
- «Сделано» пишется только после проверки (`CLAUDE.md` §2). Тесты упали — так и написать, с выводом.
- Незаконченное уходит в «что дальше», а не замалчивается.
- Даты — абсолютные.
- Пишем состояние, а не историю: «сейчас работает так», а не «было А, стало Б».

**Когда фаза закрыта целиком** (`CLAUDE.md` §9): свернуть записи в `CLAUDE.md` §6 (3–6 строк о том, что теперь есть), отступления — в §7, долгоживущие грабли — в §10, и **очистить этот файл** до шапки, шаблона и точки входа в следующую фазу. Летопись живёт в git, не здесь.

---

## Шаблон записи

```markdown
## Подфаза X.Y — <название> — <дата>

**Сделано**
- Что именно и где лежит (файлы, маршруты, миграции).
- Чем проверено: какие тесты прогнаны, какие endpoint'ы дёрнуты, какие экраны открыты.

**Принятые решения**
- Развилка → выбранный вариант → почему. Решение принял пользователь — так и написать.

**Что дальше**
- Следующая подфаза по `PLAN.md`.
- Что из текущей подфазы осталось незакрытым и почему.

**Где грабли**
- На что напоролись, что не очевидно из кода, что сломается у следующего агента, если он этого не знает.
```

---

## Записи

## Фазы 0–3 — завершены — 2–4 сентября 2026

Плюс два сеанса сквозной независимой ревизии и полное закрытие обоих отчётов.

Здесь не пересказывается: **что сейчас есть — `CLAUDE.md` §6**, согласованные отступления и осознанные отказы — **§7**, грабли — **§10**. Отчёты ревизий удалены, их содержание разошлось по коду и по этим разделам.

**Точка входа в Фазу 4** (`PLAN.md`, ТЗ §4 и §5):

- Подфаза **4.1** — backend: лента доступных заказов и предложения (`POST /offers` с upsert-семантикой §4.1, `POST /offers/:id/withdraw`).
- Перед началом прочитать в `CLAUDE.md` §10 блок **«Что учесть в Фазе 4»**: транзакция вокруг upsert предложения, обязательный `offerStatusBefore`, третий аргумент `can()`.
- Открытые вопросы §8.1 и §8.2 напоминаются в подфазе 7.4, раньше не поднимать.

Начинать только по явному указанию пользователя.

---

## Подфаза 4.1 — Backend: лента и предложения — 4 сентября 2026

**Сделано**

Модуль `backend/src/modules/offers/`:

- `POST /offers` — отправка и обновление предложения одним маршрутом (upsert по `@@unique([orderId, companyId])`, ТЗ §4.1). Запись `Offer` и переход заказа `WAITING → AWAITING_CONFIRMATION` идут **одной** транзакцией.
- `POST /offers/:id/withdraw` (роль `COMPANY`), `POST /offers/:id/reject` (роль `CLIENT`) — оба через `OrderTransitionService`, оба возвращают предложение уже в новом статусе.
- `GET /company/orders/available?q=&page=` — лента: заказы в `WAITING`/`AWAITING_CONFIRMATION`, где у компании нет предложения либо оно `WITHDRAWN`/`REJECTED`. Условие вынесено в чистый `available-orders.ts`.
- `GET /company/offers?status=&page=` — свои предложения, к каждому вложен заказ (`CompanyOfferItem`).

Изменения в существующем коде:

- `OrderTransitionService.apply(command, tx?)` принимает чужую транзакцию; `lockOrder` стал публичным; настройки транзакции вынесены в `TRANSITION_TX_OPTIONS`.
- Проверка «дата не в прошлом» вынесена из `create-order.dto.ts` в `common/validators/is-not-past-date.ts` — её использует и срок в предложении. Тексты сообщений задаются на месте применения, пользовательский текст заказа не изменился.
- Пагинация: `common/dto/pagination.dto.ts` (`PaginationQueryDto`, `SearchQueryDto`) и `common/pagination.ts` (`pageRequest`, `toPage`). `ListOrdersQueryDto` и `OrdersService.list` переведены на них.
- `buildSearchConditions(query, { includeContractor })` — в ленте поиск по подрядчику отключён: у доступного заказа исполнителя нет.
- `shared/`: `OFFER_LIMITS`, `OFFER_PRICE_PATTERN`, `OFFER_ELIGIBLE_ORDER_STATUSES`, `RESUBMITTABLE_OFFER_STATUSES`, `acceptsOffers`, `canResubmitOffer`, тип `CompanyOfferItem`.

**Чем проверено**

- `npm run typecheck`, `npm run lint` — чисто, предупреждений нет.
- `npm test`: shared 143, backend 364, frontend 166.
- `npm run test:e2e`: **106 тестов** (было 85), из них 21 новый в `test/offers.e2e-spec.ts` — живая Supabase, 86 с.
- Вручную на seed-данных под `info@archproject.mybuild.test` и `anna.client@mybuild.test`: лента, свои предложения, отправка предложения на заказ с отозванным предложением (переиспользовалась та же строка), карточка заказа глазами компании, отклонение клиентом, фильтр по статусу, 400 на цену `0` и срок в прошлом. Изменённые проверкой seed-строки возвращены в исходное состояние точечно, сервер остановлен, порт 4000 свободен.

**Принятые решения**

- **Права на `/offers/:id/*` проверяет сервис, а не `OwnershipGuard`.** Guard ищет по `:id` **заказ**, а здесь `:id` — предложение. Сервис делает это одним запросом, который всё равно нужен, чтобы узнать `orderId` для перехода. Чужое предложение отдаётся как 404, а не 403, — то же правило, что и у guard'а.
- **Порядок блокировок в `submit`: сначала заказ, потом предложение.** Переход берёт те же две строки в этом порядке; обратный порядок здесь дал бы взаимную блокировку с одновременным принятием чужого предложения. Порядок закреплён unit-тестом на трассу вызовов — на живой базе он не виден.
- **`POST /offers` возвращает `OfferDto`, а не карточку заказа.** Заказ фронт перечитает сам; отдавать его из мутации значило бы повторять `getDetail` в третьем месте.
- **Цена предложения — строго больше нуля** (`OFFER_PRICE_PATTERN`). ТЗ этого не оговаривает, но `MONEY_PATTERN` пропускает `0` и `0.00`, а работа за ноль — ошибка ввода, а не предложение.
- **`NOT_ACCEPTED` в ленту не возвращается** — ровно по букве ТЗ §4.1. Недостижимо на практике: этот статус появляется только вместе с переходом заказа в `IN_PROGRESS`.
- **Выделение общей пагинации и валидатора даты** — это не «переписал заодно»: без них третий и четвёртый список повторяли бы границы `page`/`pageSize` заново, а один забытый `@Max` возвращает 500 из Prisma (`MAX_PAGE`, находка R3-С1).

**Что дальше**

Подфаза **4.2** — backend: сделка и приёмка (`accept-offer`, `submit`, `POST /orders/:id/files`, `verified-area`, `confirm`, `dispute`). Из 4.1 незакрытого не осталось.

**Где грабли**

- **`OrderTransitionService.apply` теперь принимает второй аргумент.** Забыть его в 4.2 там, где транзакция уже открыта, — значит открыть вложенную: Prisma даст **вторую** транзакцию на другом соединении, и атомарности не будет, хотя код выглядит правильно.
- **Мок без объявленных параметров снова стоил времени** — `vi.fn(async () => …)` дал `mock.calls` типа `[]`, и `calls[0]![1]` перестал компилироваться. Грабля уже описана в `CLAUDE.md` §10, но встречается каждый раз заново.
- **e2e ленты нельзя писать «взять весь список и поискать в нём свой заказ»**: лента глобальная, в ней лежат заказы seed и соседних наборов e2e. Все проверки ленты сужены поиском по номеру заказа (`availableIds`).
- **Ручная проверка на seed портит seed.** Отправка предложения переписывает существующую строку `Offer` и создаёт уведомления. Восстанавливать точечно (обновить строку, удалить уведомления новее даты seed) — `npm run db:seed` пересоздал бы все заказы с новыми номерами.
- **Скрипт из `%TEMP%` не видит зависимостей репозитория**: и `dotenv`, и `@supabase/supabase-js`, и `PrismaClient` берутся через `createRequire`, привязанный к каталогу `backend`. `PrismaClient` вдобавок требует адаптер `PrismaPg` с `DATABASE_URL` — без него Prisma 7 падает на конструкторе.

---

## Подфаза 4.2 — Backend: сделка и приёмка — 4 сентября 2026

**Сделано**

Новая таблица `OrderSubmission` (миграция `20260904160000_order_submissions`, применена): `orderId`, `round`, `comment` (NOT NULL), `submittedAt`, `@@unique([orderId, round])`, RLS включён без политик. Одна строка на сдачу: она хранит обязательный комментарий компании и отвечает, какой сейчас номер раунда.

Маршруты (`order-workflow.controller.ts`, префикс `orders` — второй контроллер рядом с CRUD; логика в `order-workflow.service.ts`), все возвращают свежий `OrderDetail`:

- `POST /orders/:id/accept-offer/:offerId` — клиент, `OrderAccessMode.OWNER`.
- `POST /orders/:id/confirm` (комментарий необязателен), `POST /orders/:id/dispute` (`correctionComment` обязателен) — клиент.
- `POST /orders/:id/submit` — компания-исполнитель: переход и закрытие сдачи (`submittedAt`) одной транзакцией.
- `POST /orders/:id/files` — multipart, комментарий обязателен: файлы дописываются в открытую сдачу либо открывают следующий раунд; уведомление `FILES_UPDATED` клиенту.
- `PATCH /orders/:id/verified-area` — компания-исполнитель, статус и цену не меняет, уведомление `AREA_VERIFIED`.

Остальное:

- `OrderAccessMode.EXECUTOR` в `OwnershipGuard` — пускает только компанию, чьё предложение принято; владелец заказа туда не проходит.
- `OrderDetail.submissions` (`round`, `comment`, `submittedAt`, `createdAt`) — видны только сторонам сделки, как и файлы. Файлы внутрь сдачи не вкладываются: они уже в `files` и связаны полем `submissionRound`.
- `shared/`: `ORDER_LIMITS.comment`, `WORK_UPLOAD_ORDER_STATUSES`, `AREA_VERIFIABLE_ORDER_STATUSES`, `canUploadWork`, `canVerifyArea`, тип `OrderSubmissionDto`.
- Вынесены общие куски: `common/dto/transforms.ts` (`trim`, `optionalText` — были в двух DTO), `orders/multer-file.ts` (`MulterFile`, `toUploads` — теперь два загружающих маршрута), `orders/order-notification.ts` (`orderRef` — подпись заказа нужна и вне state-машины).
- `seed.ts`: у заказов с файлами компании появились сдачи с комментариями (перепланировка — раунд 1; санузел — раунд 1, вернулся на доработку; дизайн-проект — раунды 1 и 2).

**Чем проверено**

- `npm run typecheck`, `npm run lint` — чисто, предупреждений нет.
- `npm test`: shared 159, backend 395, frontend 166.
- `npm run test:e2e`: **129 тестов** (было 106), из них 23 новых в `test/workflow.e2e-spec.ts` — живая Supabase и живой бакет, 145 с. Полный цикл идёт настоящими HTTP-запросами: принятие предложения → загрузка файлов (multipart) → сдача → доработка → пересдача вторым раундом → подтверждение, плюс права (клиент/исполнитель/посторонняя компания), 409 на сдачу без файлов и на дозагрузку в закрытый раунд, приватность сдач.
- `npm run db:seed` прогнан, сдачи в базе проверены запросом: у четырёх строк верные раунды, комментарии и `submittedAt`. Временный скрипт удалён, серверов не поднималось.

**Принятые решения**

- **Хранение комментария к сдаче — отдельная таблица `OrderSubmission`** (выбор пользователя из трёх вариантов). Альтернативы — колонка у `OrderFile` (комментарий в N копиях) и «не хранить, только в уведомлении» (в карточке заказа его не показать).
- **Все шесть маршрутов возвращают `OrderDetail`.** Каждый из них меняет карточку целиком (статус, цена, сдачи, файлы), и второй запрос за ней был бы лишним. В 4.1 `POST /offers` отдавал `OfferDto` по тому же правилу: возвращается ресурс, который маршрут меняет.
- **Дозагрузка файлов в статусе `AWAITING_COMPLETION_CONFIRMATION` запрещена** (409). ТЗ этого не оговаривает, но раунд уже отправлен клиенту, и тихо менять то, что он в этот момент проверяет, нельзя. Чтобы приложить забытый файл, нужна новая сдача — то есть сначала решение клиента.
- **Сдать работу нельзя, пока в открытом раунде нет ни одного файла** (409). Тоже сверх ТЗ: сдача без файлов и без комментария ничего не сообщает, а нумерация раундов после неё разошлась бы с файлами.
- **Комментарий сдачи заменяется, а не накапливается**: он описывает сдачу целиком, а не отдельную загрузку.
- **Уведомление `FILES_UPDATED` не отправляется, если все файлы оказались дубликатами** — в заказе ничего не изменилось.
- **Проверка «исполнитель» ушла в guard, а не в сервис** (в отличие от 4.1, где `:id` — предложение): здесь `:id` — заказ, guard его уже читает и уже знает своё предложение компании. Отказ — 404, как и во всех остальных режимах.

**Что дальше**

Подфаза **4.3** — frontend: кабинет компании (`/available`, форма предложения, `/offers`). Из 4.2 незакрытого не осталось.

**Где грабли**

- **`OrderDetail.submissions` не содержит файлов.** Файлы сдачи — это `files.filter(ownerType === COMPANY)`, сгруппированные по `submissionRound`, а `submissions[].round` — ключ соединения. Группировка для 4.5 должна лечь в `lib/`, а не в компонент.
- **Открытая сдача — та, у которой `submittedAt === null`.** Их не может быть двух, но выбирать надо именно по этому полю, а не «последнюю по номеру»: после сдачи последняя закрыта, и дозагрузка в неё запрещена.
- **Загрузка в хранилище идёт после коммита транзакции.** Если она упадёт, строка сдачи с комментарием останется без файлов — это нормально: компания повторит загрузку, попадёт в ту же открытую сдачу, комментарий перезапишется.
- **`transitions.apply` внутри `submitWork` получает вторую блокировку той же строки** — это не ошибка: повторный `FOR UPDATE` в той же транзакции ничего не стоит, зато порядок блокировок остаётся одинаковым во всём коде (сначала заказ).
- **Seed теперь заводит и сдачи.** Добавив заказу файлы компании и забыв `submissions`, получишь карточку с файлами «ничьей» сдачи — и `POST /submit` ответит «нечего сдавать», хотя файлы видны.
- **`@types/multer` по-прежнему нет**, и `MulterFile` описан руками в `orders/multer-file.ts`. Новый загружающий маршрут берёт тип оттуда, а не объявляет свой.

---

## Подфаза 4.3 — Frontend: кабинет компании — 4 сентября 2026

**Сделано**

Два раздела компании перестали быть заглушками:

- `/available` — лента доступных заказов: поиск и пагинация в адресе, карточки с бюджетом клиента, категорией и типом объекта, ссылка на карточку заказа и кнопка «Отправить предложение». Цены сделки, срока и подрядчика в строке нет — чужого прогресса компания не видит (ТЗ §4.1).
- `/offers` — свои предложения: вкладки по всем восьми статусам, цена/срок/бюджет клиента, комментарий. У предложения в `SENT` — «Изменить предложение» и «Отозвать»; у остальных — подсказка о том, что происходит с заказом (`lib/offer-view.ts`).
- `components/offers/offer-dialog.tsx` — один диалог и на отправку, и на изменение (upsert ТЗ §4.1): при изменении открывается заполненным, кнопка и заголовок меняют слова. `withdraw-offer-dialog.tsx` — отзыв с подтверждением.
- Правила формы — в `lib/offer-form.ts` (цена по `OFFER_PRICE_PATTERN`, срок не в прошлом, комментарий ≤ 2000), `shared`: `isPendingOffer`.

Общее, что вынесено ради трёх списков вместо одного:

- `lib/list-params.ts` — разбор `q`/`page`/статуса и сборка адреса; на нём переписан `orders-filter.ts` (публичный API не изменился) и написаны `available-filter.ts`, `offers-filter.ts`.
- `components/list-parts.tsx` (`PaginationBar`, `EmptyCard`, `OutOfRange`, `CardListSkeleton`), `components/list-search.tsx`, `components/status-tabs.tsx`. `orders-search.tsx` удалён, `orders-status-tabs.tsx` стал сборкой вкладок.
- `lib/api-errors.ts` — разбор ответа API в текст; на него переведены форма заказа и диалог удаления (было три копии одного кода).
- `lib/form-input.ts` — `normalizeNumber`, `todayIsoDate`, `isCalendarDate`, `isPastDate`; заказ и предложение спрашивают сумму и дату по одним правилам.

**Чем проверено**

- `npm run typecheck`, `npm run lint` — чисто.
- `npm test`: shared 167, backend 395, frontend 223.
- `npm run test:e2e`: 129 тестов пройдено. Прогон завершился ошибкой **уборки** (`dropUsers` в `offers.e2e-spec.ts`: `fetch failed` к Supabase Admin API). Повторный прогон этого файла — 21 тест и уборка прошли, значит сбой сетевой, а не в коде.
- В браузере под `info@archproject.mybuild.test`: лента, отправка предложения (валидация пустой формы, цена с запятой, календарь не даёт прошлое), заказ ушёл из ленты; на `/offers` предложение с ценой и комментарием; изменение цены (форма открылась заполненной); отзыв — статус сменился, кнопки исчезли, появилась подсказка; заказ снова нашёлся в ленте поиском по номеру. Проверены пустые состояния обоих списков и мобильная ширина 390 px эмуляцией (горизонтальной прокрутки нет). Под `anna.client@mybuild.test` проверено, что список заказов после рефакторинга цел: поиск, вкладка статуса, пагинация.
- В консоли только известное предупреждение `next/image` (отложено в 7.2).
- После проверки: Chrome закрыт, серверы остановлены, порты 3000 и 4000 свободны, база возвращена `npm run db:seed`.

**Принятые решения**

- **Карточку заказа (`/orders/[id]`) в этой подфазе не трогал.** Блок «Ваше предложение» и действия компании там — 4.5, где страница собирается целиком вместе со сдачами; делать половину блока сейчас значило бы переписывать его через подфазу. Компания открывает заказ ссылкой из ленты, а кнопка отправки живёт в самой ленте.
- **Кнопки «Отправить заново» у отозванного и отклонённого предложения нет.** Настоящий статус заказа компании не виден (ТЗ §4.1): заказ, который уже выполняет другая компания, выглядит для неё как «ищет исполнителя», и кнопка обещала бы то, на что сервер ответит 409. Вместо неё — ссылка в ленту, которая считает доступность на сервере.
- **Изменять и отзывать можно только предложение в `SENT`** (`isPendingOffer` в `shared/`) — тот же список, что в `OFFER_PRECONDITIONS` state-машины.
- **Вкладки подписаны полными названиями статусов из §3**, хотя они длинные. Заводить второй, короткий набор названий значило бы разойтись с ТЗ («выводятся как есть»); длину решает горизонтальная прокрутка вкладок.
- **Выделение общих частей списков — не «переписал заодно».** Третий список повторил бы границы `page`/`q`, разметку пагинации и пустых состояний в третий раз; забытый потолок `MAX_PAGE` возвращает 400 вместо пустой страницы. По той же причине в 4.1 выносилась пагинация backend.
- **Лента — карточки, а не таблица** (в отличие от списка клиента): у каждой строки своя кнопка действия, а бюджет клиента — то, ради чего компания в ленту и смотрит.

**Что дальше**

Подфаза **4.4** — frontend: клиент выбирает и принимает предложение (список предложений на карточке заказа, «Принять» / «Отклонить», подтверждение и доработка). Из 4.3 незакрытого не осталось.

**Где грабли**

- **Функцию нельзя передать из серверного компонента в клиентский.** `ListSearch` поэтому получает `basePath` и `params` данными и собирает адрес сам; `PaginationBar` принимает `hrefFor` только потому, что сам остаётся серверным. Попытка передать сборщик адреса в клиентский компонент падает в рантайме, а не на типах.
- **`formatMoney` ставит неразрывный пробел (U+00A0).** Проверка вида `innerText.includes("47 000 USD")` с обычным пробелом даёт ложное «не нашлось» — при проверке в браузере сравнивать по номеру заказа или искать с ` `.
- **Отзыв предложения возвращает заказ в `WAITING` только если это было последнее активное предложение.** На seed-данных ORD-443 после отзыва остался в `AWAITING_CONFIRMATION` — по нему есть предложение другой компании. Это ТЗ §4, а не ошибка: не искать заказ во вкладке «Поиск исполнителя» сразу после отзыва.
- **Диалог сбрасывает форму на открытии, а не на закрытии.** Между двумя открытиями страница могла обновиться, и сохранённый черновик показывался бы как действующая цена.
- **`npm run db:seed` требует запуска вне песочницы агента** (грабля §10 про Prisma CLI) — иначе `P1001`. Восстанавливать состояние после ручной проверки лучше им целиком, чем точечными правками строк.

---

## Подфаза 4.4 — Frontend: клиент выбирает и принимает — 4 сентября 2026

**Сделано**

Правила state-машины переехали в `shared/src/state.ts`: `OrderEventType`, `ORDER_TRANSITIONS` (таблица переходов ТЗ §4), `OFFER_PRECONDITIONS` и чистая `canTransition(status, event, offerStatus?)`. Обработчики переходов остались на backend, но их таблица теперь типизируется общей: `TransitionTable` выводит набор ключей из `ORDER_TRANSITIONS`, `OrderStateMachine.can` делегирует в `canTransition`, а допустимость события в `transition()` решает общая таблица, а не наличие обработчика. Источник правды один, и состав кнопок на фронте считает та же функция, что и сервер.

Карточка заказа `/orders/[id]` для клиента перестала быть заглушкой:

- `components/orders/order-offers.tsx` — активные предложения списком (название компании, цена, срок, комментарий, дата) с кнопками у каждого; после выбора тот же блок показывает «Принятое предложение» со статусом. Пустое состояние объясняет, откуда предложения возьмутся.
- `components/orders/offer-decision-dialogs.tsx` — «Принять» и «Отклонить» с подтверждением. В принятии видны цена и срок будущей сделки и число предложений, которые уйдут в «Не выбрано».
- `components/orders/completion-card.tsx` + `completion-dialogs.tsx` — блок «Приёмка работы»: подсказка по статусу, `correctionComment` и комментарий приёмки, кнопки «Подтвердить выполнение» (комментарий по желанию) и «Отправить на доработку» (обязателен).
- Логика — в `lib/`: `order-actions.ts` (`resolveClientActions` — решения по каждому предложению, предложение исполнителя, флаги приёмки), `completion-view.ts` (`completionHint` на все шесть статусов, разный для клиента и исполнителя), `completion-form.ts` (зеркало `ConfirmOrderDto`/`DisputeOrderDto`).
- Заглушки `ComingSoon` сузились: «Ваше предложение» и «Сдачи работ» видны только тем, кому в 4.5 будет что показать (стороны сделки, компания со своим предложением).

**Чем проверено**

- `npm run typecheck`, `npm run lint` — чисто.
- `npm test`: shared 262, backend 395, frontend 244.
- `npm run test:e2e`: 129 тестов пройдено (переезд таблиц ничего не сломал).
- Отдельно проверено, что связка типов работает: удаление обработчика `WORK_DISPUTED` из backend-таблицы валит `tsc` (`TS2418`). Лишний обработчик компилятор **не** ловит — отсюда проверка `canTransition` в `transition()`.
- В браузере под `anna.client@mybuild.test`: ORD-449 — список из двух предложений, отклонение одного (список стал из одного), принятие второго (заказ «В работе», блок «Принятое предложение», цена сделки 49 500 USD, срок и подрядчик проставлены, кнопка удаления исчезла); ORD-451 — отправка на доработку с пустым комментарием отбита формой, с текстом прошла (статус «На доработке», комментарий на странице, кнопки исчезли), затем подтверждение выполнения с комментарием (статус «Завершён», оба комментария видны); ORD-448 — «Предложений пока нет»; ORD-452 — подсказка про доработку без кнопок.
- Под `info@stroygrad.mybuild.test`: на своём заказе кнопок решения нет и чужих предложений не видно; на чужом заказе (ORD-451) — статус «Поиск исполнителя», ни предложений, ни приёмки, ни файлов.
- Мобильная ширина 390 px эмуляцией: `innerWidth` 390, `scrollWidth` 390, горизонтальной прокрутки нет. В консоли ошибок и предупреждений нет.
- После проверки: Chrome закрыт, серверы остановлены, порты 3000 и 4000 свободны, временный скрипт удалён, база возвращена `npm run db:seed`.

**Принятые решения**

- **Таблицы переходов вынесены в `shared/`** (выбор пользователя из двух вариантов). Альтернатива — отдельные предикаты вида `canAcceptOffer` рядом с `canUploadWork` — записала бы факты таблицы переходов во второе место. Здесь же расхождение ловится компилятором.
- **`canTransition`, а не `can`.** Имя `can` через `export *` попадает в `@/lib/types` и оттуда во все компоненты — слишком общее для глобального. Метод машины остался `can`, ТЗ и `CLAUDE.md` §10 говорят о нём же.
- **Допустимость события в `transition()` проверяется по `ORDER_TRANSITIONS`, а не по наличию обработчика.** У вычисляемых ключей TypeScript не делает проверку на избыточность, и лишняя строка в таблице обработчиков разрешила бы на сервере то, чего интерфейс не показывает.
- **Проверка `isOwner` в `resolveClientActions` не избыточна.** Своё предложение в `SENT` компания видит в том же поле `offers`, и по одним лишь статусам получила бы кнопку «Принять» на саму себя.
- **`canConfirmWork` требует предложения исполнителя.** Без третьего аргумента `canTransition` проверяет только статус заказа и ответил бы «да» на заказе без предложений.
- **Блок «Приёмка работы» показывается обеим сторонам, кнопки — только клиенту.** `correctionComment` по ТЗ §4.1 виден нужной стороне, и оставлять исполнителя без текста доработки до 4.5 значило бы показать ему заказ «на доработке» без единого слова о том, что не так. Кнопки компании (сдать работу) добавит 4.5 в эту же карточку.
- **Название компании в заголовках диалогов без кавычек**: у большинства они уже в самом названии, и «ООО «Ремонт Плюс»» читается как опечатка. Нашлось при проверке в браузере.

**Что дальше**

Подфаза **4.5** — frontend: сдача работы и история сдач (загрузка файлов с обязательным комментарием, «Сдать работу», уточнение площади, история сдач, сквозная проверка DoD Фазы 4 на двух аккаунтах). Из 4.4 незакрытого не осталось.

**Где грабли**

- **Кнопку «Подтвердить выполнение» на seed-данных проверить нечем**: заказ в `AWAITING_COMPLETION_CONFIRMATION` один (ORD-451), и первая же проверка доработки уводит его в `COMPLETION_DISPUTED`. Вернуть его обратно двумя `UPDATE` через временный скрипт — быстрее, чем гонять полный цикл; в конце всё равно `npm run db:seed`.
- **Временный скрипт с Prisma берёт клиент из `backend/dist`, а не из `src`.** В `src/generated/prisma/` лежат `.ts`, и `import` из `.mjs` их не видит — путь работает только после сборки backend.
- **`npm run db:seed` пересоздаёт учётные записи Supabase.** Открытая в браузере сессия после него становится недействительной — профиль по `sub` из старого токена не находится, и весь кабинет падает на «Профиль не найден». Лечится входом заново, но cookie httpOnly не сбросить из консоли: сначала `document.cookie` с `Max-Age=0`, потом `/login`.
- **`ComingSoon` теперь адресный.** Заглушка «Ваше предложение» показывается только компании, у которой предложение есть; «Сдачи работ» — только сторонам сделки. Добавляя блок в 4.5, эти же условия надо перенести на настоящую карточку, а не оставить их у заглушки.
- **Проверка «лишний обработчик» стоила времени зря.** Excess property check не работает на вычисляемых ключах (`[OrderEventType.X]: …`), поэтому типами эта половина не закрывается — только рантаймом.
